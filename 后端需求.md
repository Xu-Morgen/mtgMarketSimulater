# 后端需求

本文件定义服务端的经济真相、事务和异步任务。完整技术栈见[技术栈与模块职责边界.md](技术栈与模块职责边界.md)。

## 1. 技术与运行要求

- Fastify + OpenAPI；SQLite（WAL 模式）+ Drizzle ORM；SQLite `jobs` 表与进程内任务循环；Docker Compose 部署。HTTP 层统一返回 contracts 响应包络、回传请求 ID、使用结构化 Pino 脱敏日志，并仅允许配置的 CORS Origin。
- 所有余额、库存、订单、保证金、开包、比赛奖励与成就写操作仅由服务端完成。
- 关键结算使用 SQLite 短事务、唯一约束、条件更新和 idempotency key；所有经济变动写追加式审计流水。单实例串行处理结算，避免 SQLite 写竞争。
- 数据库 schema 与 SQL 迁移统一由 `packages/database` 管理；启动先执行版本化迁移与 `PRAGMA integrity_check`。每个迁移必须短事务原子提交，失败时不写该迁移记录且保留迁移前状态。

## 2. 业务模块

| 模块 | 必须职责 |
| --- | --- |
| 用户与认证 | 账户、会话、角色、状态、初始资金与每日工作资金；以用户+自然日唯一约束防重复发钱；为管理员提供用户检索、冻结/解冻、会话撤销和只读经济摘要。 |
| 卡牌目录 | 后端同步 Scryfall 卡牌与印刷版本，生成可管理 SKU。 |
| 外部价格与 MTGJSON 导入 | 导入 MTGJSON、校验映射、只追加价格快照；`AllPrices` 历史回填只补齐缺失日期。系列、密封产品与 booster 资料仅作为管理员审核的导入草稿，异常时保留最后成功版本。 |
| 市场与 NPC | 消费已结算的开包/NPC/P2P 事实事件，使用规则引擎计算参考价、供需聚合、系列/关联/事件系数、NPC 买卖价、价差与限额。 |
| 库存与开包 | 管理可用/锁定库存；开包、扣款、库存变化在同一事务完成。 |
| 卡组与比赛 | 首发实现带 Companion、单/双官方合法指挥官的 100 张 Commander 服务端合法性、官方禁牌版本快照、报名时生成的评分来源快照和报名锁定；负责按服务器时区刷新 NPC 日赛、玩家创建赛事、确定性/现实桌全桌确认积分结算、奖励临界位加赛、排名池/随机池奖励与重放审计。I24R 先验证本地评分模型是否可替代或降级 Leyline；任一评分只在报名时经服务器适配持久化为快照，绝不在结算时联网。 |

## 2.1 I07B 存档、账户与账本（已完成，2026-07-24）

- `POST /v1/archive` 在用户、存档、账户、初始 `initial_funding` 流水、业务审计与幂等完成记录之间采用单个 SQLite 短事务；`game_archives.user_id`、账户币种与 `(actor_id, idempotency_key)` 唯一约束共同防止重复发放。
- 初始资金规则为 `initial-funds/v1`，从 `@mtg-market/rules` 解析，定义同步存入 `rule_versions`；每份存档保存使用的规则版本。余额只以 `GAME_CREDIT` 整数最小单位保存，数据库强制总额等于可用额加冻结额且不为负。
- users application 提供 reserve/release/capture 共享资金原语。冻结记录必须关联业务实体和原因；扣除必须由 active hold 发生并新增 debit 账本。任何未来订单或履约操作不得直接更新账户字段。

## 2.2 I10B 库存、成本与资产锁定（已完成，2026-07-24）

- `inventory_holdings` 以 `(user_id, sku_id)` 唯一记录持有量、可用量、订单锁定量、比赛锁定量、整数移动平均成本和可空市值快照；数据库约束强制持有量等于三类数量之和且全部非负。
- `inventory_holds` 保存订单/比赛的 active、released、captured 状态及关联实体，`inventory_entries` 为不可变数量与成本流水。锁定使用条件更新；释放或 capture 仅可消费 active hold，因此重放不会超额解锁或产生幽灵库存。
- `InventoryService.withLedgerTransaction` 明确要求开包、订单和比赛将库存、账本、事实事件及审计置于同一 SQLite 短事务。玩家 API 只提供当前用户的库存总览、单卡持仓与可分页对账查询；不提供直接改库存、成本或锁定量的接口。

## 2.3 I11B 补充包配置与可重放规则（已完成，2026-07-26）

- `booster_packs` 保存补充包商品价格、启用状态、停用原因与当前规则版本；`booster_pack_rules` 以 `(pack_id, version)` 保存不可变的完整卡位、候选池、SKU 整数权重定义。规则包负责校验边界、计算概率及以明确种子生成确定性结果；MVP 没有任何保底、计数器或跨包状态字段。
- `GET /v1/packs` 与详情路由只公示服务端计算的稀有度概率、价格、启用状态和规则版本，绝不交付候选池、种子或开奖结果。配置写入/发布管理命令仍归 I30B 管理模块，I11B 不提供浏览器开奖命令。
- `PackService` 只在服务端用 CSPRNG 生成 32-byte 种子，并将原始种子、SHA-256 与规则结果摘要追加到无 HTTP 读取路径的 `pack_rule_replays`。I12B 必须在既有库存/账本共同事务中调用此能力，不能将种子、结果、扣款或库存拆分提交。
- 迁移 `0013_base_bro_sos_packs.sql` 创建 `BRO-BASE`、`SOS-BASE` 两款 500 游戏币基础补充包，初始停用以等待目录。成功的 `catalog.sync` 在替换 Scryfall 目录的同一短事务内调用 packs application，为当前系列的非闪 `common`/`uncommon`/`rare`/`mythic` SKU 追加新规则快照：10 普通、3 非普通、1 张按 7:1 稀有/秘稀选择的卡位；缺少必要稀有度时保持停用。同步重跑只追加并切换版本，绝不覆盖旧规则或已结算记录。

## 2.4 I12B 商店购买与服务端开包（已完成，2026-07-26）

- `GET /v1/store/packs`、购买预览、`POST /v1/packs/{packId}/open` 与当前玩家的开包历史共同构成商店协议。购买请求回传服务端预览中的规则版本并携带幂等键；下架、失效 SKU、版本过期、未建档或余额不足均在扣款前拒绝。
- `pack_openings` 与 `pack_rule_replays` 一对一关联，追加规则版本、消费额和不含种子的结果摘要。开包用例使用 inventory application 事务编排，同时追加 `pack_purchase` 账本、`pack_opened` 库存流水、`pack.opened` 事实事件/outbox 与业务审计；故障不能留下任何半完成记录。
- I17B 价格/市场模块上线前，结果 DTO 的参考价、游戏内价和盈亏都返回 `null`，并以 `unavailable_until_i17` 说明状态；事实事件只供后续消费，不在本期生成市场报价。

## 2.4b I33B 收藏图鉴与开包体验服务端（已完成，2026-08-04）

- 收藏图鉴只读聚合 `GET /v1/collection/album`：按系列分组图鉴、每系列已收集/全部印刷×工艺 SKU 数与完成度 bp、未收集卡位列表（卡名/系列/编号/稀有度，供灰影占位），支持 `onlyHeld=held` 与游标分页；数据全部来自目录与库存快照，不写经济表。
- 开包结果 DTO 扩展：每张开出的卡返回 `isNewToCollection`（本次结算前该 SKU 是否已持有）与 `collectionProgressAfter`（开包后所在系列完成度快照）；`PackOpeningDto` 返回本包 `totalCost`（= spent）与 `totalGameValue`（按结算时已持久化报价快照计算，任一缺价则为 null）。浏览器不得自行比对库存推导新卡或盈亏。
- 批量开包 `POST /v1/packs/bulk`（10/50/100）：单事务批量结算，每包独立 replay/扣款/入库/开包记录/`pack.opened` fact/outbox/重定价任务，汇总各稀有度计数、总成本、总价值与新增 SKU 数；任一包失败整批回滚，幂等与费用约束复用单包语义。
- 重复卡批量卖出 `POST /v1/inventory/duplicates/sell`：只卖持有量 >1 的可用库存（保留至少一张），逐 SKU 复用 NPC 卖出规则、报价快照与每日额度，账本+库存流水+成交+事件单事务，返回张数/收入/费用汇总与跳过原因；不提供直接删除卡牌能力。
- 特殊补充包（C6）：后台可配置限时/折扣包（`pack_offers` 限时销售窗口，折扣价 = price × discountBps ÷ 10_000），有 offer 的包只在窗口内可购买，窗口外/过期与下架同语义拒绝；主题包的限定卡池/概率覆盖仍由不可变 `booster_pack_rules` 版本承载。
- 系列收集率里程碑成就：`set-completion-80/v1`/`set-completion-100/v1` 按系列完成度 bp 自动解锁发放，`pack-achievement.process` 幂等消费 `pack.opened` fact，复用 `achievement_unlocks`/`achievement_reward_grants` 唯一约束防重复发奖。
| P2P 订单 | 双边委托确认、资金/库存锁定、撮合、模拟履约期限、保证金、取消与风控。 |
| 卡组与比赛 | 校验带 Companion、单/双官方合法指挥官的 100 张 Commander 库存、当前官方禁牌快照与赛制，保存不锁库存的草稿，报名时生成评分来源快照并锁定参赛卡牌，确定性赛果/现实桌全桌确认积分、排名池/随机池奖励、成就触发与重放信息。 |
| 管理与审计 | 管理员仪表盘聚合；活动草稿、预览、发布/定时发布、暂停和结束；用户管理命令；参数版本化；只读、分页、脱敏的后台/经济/任务/异常交易日志查询。I33 发布后若启用 AI，再追加 Agent 日志查询。 |

## 3. 异步任务

- `catalog.sync`：使用 Scryfall Bulk Data 同步卡池，按需写入本地卡图缓存；失败不删除现有目录或图片。
- `prices.sync`：每日同步 MTGJSON；I17B 起由 task runner 内嵌日切轮询以自然日唯一键调度与启动补跑，成功后以快照唯一键投递 `market.reprice`，失败继续使用最近成功快照并告警。独立的 `prices.backfill` 一次性 `AllPrices` 历史回填只补齐缺失日期，不替代或重写日常同步、不移动最近成功指针。
- `daily.rollover`：按服务器配置时区的自然日开放工作资金和刷新 NPC 每日赛事，必须幂等；玩家创建赛事不由日切重置。
- `market.reprice`：根据价格快照、供需事件、系列周期、关联卡、流动性与运营事件生成有界游戏报价；失败保留最近成功报价。
- `tournament.settle`：结算游戏内赛事赛果、积分/排名/加赛、可叠加的排名池/随机池奖励，释放比赛锁定并追加 `tournament.settled`；成就由该事实的幂等消费者处理。I33 发布后若启用 I34，才为每个成功结算的报名实例投递固定中文个人 AI 战报任务。
- `pack-achievement.process`：I33B 新增，幂等消费 `pack.opened` fact，为系列收集率里程碑成就推进进度/解锁/发奖（`pack-achievement.process:{factEventId}` 唯一键，任务至少执行一次、业务结果至多一次）。
- `narrative.generate`：I33 发布后的 I34 可选任务；调用 AI 或生成模板战报，绝不回滚比赛结果。首发不投递或处理此任务。
- `order.expire`：处理过期委托、模拟履约时限和保证金。
- `backup.create`：生成并校验 SQLite 一致性备份，执行保留策略；失败不得删除最近成功备份。

### I23B 每日工作资金与日切（已完成，2026-07-29）

- `APP_TIMEZONE` 必须是有效 IANA 时区；`DAILY_WORK_FUNDING_RULE_VERSION` 由服务器配置。`daily-work-funds/v1`/`v2` 是 `packages/rules` 的纯、整数规则，金额与规则版本在日切行中快照，配置切换绝不改写已开放日期或已领取记录。
- `daily.rollover` 以 `daily.rollover:<YYYY-MM-DD>` 去重投递，其 payload 固定自然日、时区与规则版本；worker 只追加 `daily_rollover_runs` 的资格记录，不遍历用户或直接入账。停机时只补投当前日，历史排队任务仍使用其原始日期，不会错发为运行当天。
- `POST /v1/daily-work-funding/claim` 在一个短 SQLite 事务中检查当日资格与存档，写 `daily_work_funding_claims`（`UNIQUE(user_id,natural_date)`）、`daily_work_funding` credit 账本、业务审计与已完成幂等响应；同键重放返回首次结果，不同键的同日重复领取拒绝。`GET /v1/daily-work-funding` 仅投影服务器的资格、规则、金额、领取结果和下一次资格时间。
- 比赛刷新仍未实现；I25B 将作为另一子处理器复用 `daily_rollover_runs`，不得让日切重置玩家创建赛事。

### I09B 目录同步实现（已完成，2026-07-24）

- `catalog.sync` 仅由服务端任务处理器调用 Scryfall Bulk Data。它使用服务端 `SCRYFALL_USER_AGENT`（不得使用 Node 默认值）请求元数据、Bulk 文件和卡图，兼容旧版 `download_uri` 数组文件及新版 `jsonl_download_uri` gzip JSONL，并可从直接描述器或 `/bulk-data` 列表选择 `default_cards`；它按对象/行扫描、只保留启用系列，避免完整 Bulk 文件超出 Node 字符串上限。它记录来源版本、下载 URI、SHA-256、启用系列、导入差异、运行时间和失败摘要；`catalog_sync_state` 永远指向最近一次成功运行。
- 导入先验证下载 JSON、必要印刷字段、Scryfall UUID、支持的工艺和重复印刷；随后在单个 SQLite 短事务内按 Scryfall 印刷 ID 和 `(printing_id, finish)` 更新系列、印刷、SKU 和图片元数据，保留 SKU ID 及其库存、价格、订单和历史事实引用。来源快照不再包含的行不得删除；差异中的 `removed` 只表示该快照已移除。任一失败不删除上次成功目录或状态指针。
- 仅 payload 中明确指定的卡图会下载到 `CATALOG_DATA_DIR/images`；文件名受 UUID/扩展名白名单约束，玩家只经认证后的本地静态路由读取，浏览器不访问 Scryfall。
- `/v1/admin/catalog/sync` 的读写都要求 `admin` 角色；写入要求 `Idempotency-Key` 并以任务唯一键重放同一次投递。固定夹具覆盖 checksum、截断、重复、缺字段、非法图片路径和事务中断。

### I13B MTGJSON 价格快照实现（已完成，2026-07-26）

- `prices.sync` 仅由服务端任务调用 `AllPricesToday` 和 `AllPrintings` 及各自 `.sha256` 侧车文件，记录两份文件的版本、SHA-256、校验状态与脱敏失败摘要。AllPrintings 解压后逐张解析 `cards` 对象，不得将整份文件转换为单一 JavaScript 字符串。其 `identifiers.scryfallId`、卡片 UUID 和 `nonfoil`/`foil`/`etched` 工艺共同映射到本地 SKU；只采集 Cardmarket `EUR` retail 中相应价格类型的最新日期正值并换算为整数欧分。
- 校验或写入失败时，`PriceSyncService` 通过任务进程的结构化日志输出同步批次、任务 ID/尝试次数、来源版本、校验阶段、关联文件及预期/实际 SHA-256；日志不得包含下载 URL、Provider 原始响应、密钥或 Cookie。`price_sync_runs` 继续只保存可向管理员展示的脱敏失败摘要。
- `price_sync_runs`、`price_sku_mappings` 与 `price_snapshot_entries` 均只追加；成功导入在一笔短事务内追加每个 Scryfall SKU 的 `priced`、`no_price` 或 `mapping_failed` 状态并更新 `card_skus.tradable`，随后才移动 `price_sync_state`。零价、无价、币种不符、缺失/歧义映射均暂停新增交易；下载、checksum、解析或事务失败不会替换最近成功快照。
- `/v1/admin/prices/sync` 的读写均要求 `admin`；POST 需要 `Idempotency-Key`，可用两份 expected SHA-256 固定受控导入。仅当最近运行的稳定失败码为 `CHECKSUM_MISMATCH` 时才允许 `allowChecksumMismatch: true` 覆写；该请求与成功快照分别留下专门审计事实和 `bypassed` 校验状态。固定夹具覆盖 normal/foil/etched、EUR、日期、零价/缺失、重复映射、checksum、覆写与事务中断；成功后以同步运行 ID 投递 I14B 的唯一 `market.reprice`，仍不改写库存估值。
- I13F 补充只读的 `/v1/prices/status`：任意已认证玩家只能读取来源、最近成功更新时间和服务端新鲜度枚举；当前同步失败而保留旧成功快照时标为 `stale`，无成功快照时标为 `unavailable`。版本、校验和、统计、任务和失败原因仍禁止离开 admin API。

### I14B 市场规则、报价与 NPC 报价（已完成，2026-07-27）

- `@mtg-market/rules` 的 `market/v1` 将 Cardmarket EUR 欧分经整数 bp 兑换为 `GAME_CREDIT`，以明确 half-up 舍入计算有界游戏内中间价、NPC 买入/卖出价和费用；规则拒绝浮点、未知版本、无效最低价/价差/费用及越界系数，并保留每项计算原因。
- `MarketService` 消费 `pack.opened`、`npc.trade.settled`、`p2p.trade.settled` 与 `tournament.settled` 的已结算事实（当前开包/NPC/P2P 产生供需和流动性压力），再组合系列周期、SKU 关联和仍在 UTC 生效期内的 scoped 市场事件。它只追加 `market_quotes` 投影，绝不回写 `price_snapshot_entries`、库存、余额或账本。
- 迁移 `0016_market_quotes.sql` 保存版本化参数、系列周期、关联、带作用范围/生效区间/原因/5,000–20,000 bp 上限的事件，以及含输入参数、原因、外部快照和规则版本的报价。`(sku_id, trigger_key)` 唯一约束保证价格同步或事实事件重放不会重复叠加。
- 成功 `prices.sync` 以 `price-sync:<runId>`、成功 `pack.opened` 以 `fact-event:<eventId>` 投递重定价；任务失败时保留最近成功报价并按 jobs 重试。`GET /v1/market/quotes/{skuId}` 与 `/v1/market/index` 仅向认证用户提供持久化结果；无有效报价返回 `PRICE_UNAVAILABLE`。新交易资格仍由价格同步的 `card_skus.tradable` 决定，旧持仓不因报价投影失败被删除。

### I15B NPC 买入结算（已完成，2026-07-27）

- `GET /v1/npc-trades/buy/{skuId}/preview` 按数量返回服务端报价快照 ID、规则版本、内含费用的单位/总价、有效期和服务器交易额度；`POST /v1/npc-trades/buy/{skuId}` 只接受该报价标识、版本、数量与玩家确认的最高单位价，金额与费用绝不来自浏览器。
- 迁移 `0017_npc_trades.sql` 创建只追加的 `npc_trades` 与受控 `npc_trade_limits`。每笔成交固定引用 `market_quotes.id`、规则版本、价格、费用、总额和 UTC 结算日；单笔及用户/SKU/日额度均由服务器检查，日额度以已结算记录聚合。
- `market/v1` 以报价计算时间生成固定 15 分钟有效期，不将日外部快照的采集时刻误作交易有效截止；`NpcTradeService` 在一个短 SQLite 事务内通过 market/users/inventory application 接口完成报价校验、`npc_buy` 账本、库存增加与成本、成交记录、`npc.trade.settled` 事实/outbox、唯一重定价任务、审计和幂等响应。报价不可用、过期/限价或版本不符、余额不足、额度超限及库存写入异常不会留下半完成结算。

### I16B NPC 卖出结算（已完成，2026-07-27）

- `GET /v1/npc-trades/sell/{skuId}/preview?quantity=<正整数|all>` 返回服务端选择的 NPC 收购报价、当前可用库存、解析后的数量、收入/内含费用、有效期和额度；`all` 只解析当前 `availableQuantity`，订单或比赛锁定库存绝不参与出售。确认命令仍回传预览解析后的确切正整数数量。
- `POST /v1/npc-trades/sell/{skuId}` 只接受报价标识、规则版本、数量和玩家确认的 `minUnitPrice`。服务端只从不可变 `market_quotes` 的 NPC 收购价读取金额，校验可交易资格、有效期、版本、最低可接受价、可用库存与单笔/单日额度；同键异参、并发重放、报价变化和库存不足均不会产生第二笔或半完成成交。
- 成功卖出在一个 `InventoryService.withLedgerTransaction` 内扣减可用库存并写 `npc_sell` 库存流水、增加余额并写 `npc_sell` credit 账本、追加 `npc_trades`、`npc.trade.settled` 事实/outbox、唯一重定价任务、审计和幂等响应。任一库存/账户/事件写入失败会回滚经济变更及幂等占位。

### I17B 价格历史、每日同步与 AllPrices 回填（已完成，2026-07-27）

- 价格历史天然只追加：每次成功的每日 `prices.sync` 都生成新的 `price_sync_runs` 行与 `price_snapshot_entries` 子行（`UNIQUE(sync_run_id, sku_id)`），每次 `market.reprice` 都生成新的 `market_quotes` 行（`UNIQUE(sku_id, trigger_key)`）。`GET /v1/market/quotes/{skuId}/history?range=7d|30d|all` 与 `GET /v1/market/index/history?range=7d|30d|all` 按自然日采样，同日多次同步/重定价取该日最新值；任一价格缺失为 null，空历史返回空 points 数组而非 404。
- 每日同步接入日程：`ensureDailyPriceSyncScheduled` 由 `startTaskRunner` 的 5 分钟节流轮询调用，以 UTC 自然日为唯一键（`prices.sync:daily:<date>` + `price_sync_schedule_state.last_scheduled_date`）投递 `prices.sync`。停机多日只补投一次而非逐日补投；同日重放不重复投递。`daily.rollover` 的发钱/赛事刷新按 AT-10B 延后至 I23B/I25B。
- 一次性 `AllPrices` 历史回填：`prices.backfill` 任务下载独立 `MTGJSON_ALLPRICES_ENDPOINT`，SHA-256 校验后按每 SKU 最新成功映射只追加缺失的历史日期快照（`run_kind='backfill'`，每日期一个独立子 run 以复用 `UNIQUE(sync_run_id, sku_id)`）。监督 run（`mapping_uri='supervisor'`）汇总统计与日期范围；绝不覆盖已有每日同步快照、移动 `price_sync_state`/`price_sync_schedule_state` 指针，或为历史日投递 `market.reprice`。
- 回填运行记录来源版本、checksum、日期范围、插入/跳过统计与审计。解析、校验、映射或事务失败时整笔回滚，不留半批次条目；已存在价格快照与日常同步保持可用。
- 数据源说明：`PublicPriceStatusDto.disclaimer` 由服务端固定为“外部参考价来自 MTGJSON / Cardmarket EUR 快照，游戏内价为虚拟货币 GAME_CREDIT；均为非实时、非真实资产”，浏览器只展示不自行拼接。

### I18B P2P 双边委托预览与订单创建（已完成，2026-07-27）

- `GET /v1/orders/buy|sell/{skuId}/preview` 由服务端以当前 `market_quotes.market_price` 为锚点、`bilateral_order_limits.limit_price_band_bps` 计算限价带（下限不低于 `market_parameters.minimum_price`），并按 `order_fee_bps`/`fulfillment_deposit_bps` 计算费用拆分；预览返回可用资金/库存、预计支出/到手、`previewVersion`（报价 ID+版本+方向+数量+带+预占金额派生）与有效期。浏览器不得自报费用、保证金或限价带。
- `POST /v1/orders/buy|sell/{skuId}` 只接受未过期 `previewVersion`、`quoteVersion`、`quoteId` 与玩家确认的限价，并要求 `Idempotency-Key`。买单原子预占 数量*限价+order_fee（走 `fund_holds`，`entity_type='bilateral_order'`）；卖单锁定库存（走 `inventory_holds`）并只预占 fulfillment_deposit，order_fee 留到 I19B/I20B 撮合/履约时从卖方收入扣除。任一预占失败回滚，不留半完成委托。
- 迁移 `0019_bilateral_orders.sql` 追加受控 `bilateral_order_limits`（单笔/单日额度、限价带、手续费率、保证金率、TTL）与只追加语义的 `bilateral_orders`（状态机 `open/partially_filled/matched_pending_fulfillment/fulfilled/cancelled/expired`、乐观版本、引用不可变报价、预览版本与 expires_at）。
- `GET /v1/orders`、`GET /v1/orders/{orderId}` 只返回当前玩家的委托；`POST /v1/orders/{orderId}/cancel` 以幂等键 + 状态版本条件 UPDATE 推进状态机，撤单释放未成交资金（买单）或库存+保证金（卖单），重放返回首次结果，重复撤单返回 `RESOURCE_CONFLICT`。
- `GET /v1/orders/book/{skuId}` 返回只读订单簿：买单按价格降序、卖单按价格升序聚合 `limitPrice/remainingQuantity/orderCount`，不含用户身份。
- `GET /v1/orders/trades?skuId=&cursor=&limit=`（player 角色）分页返回当前玩家作为买方或卖方的成交 `PlayerBilateralTradeDto`：脱敏对手 userId、对手 orderId 与所有 holdId，只返回当前玩家自己的 `myOrderId`、`role`（buyer/seller）、成交价（取 maker）、本方已成交 order_fee、已转入待履约的资产与 `fulfillmentDeadline`（待履约期限）。纯读、不写事务、不引幂等键、不写审计；浏览器只展示服务端状态，连接失败时提示数据可能过期。
- `POST /v1/orders/trades/{tradeId}/fulfill`（player，请求体为空，需 `Idempotency-Key`，买卖任一方均可发起）确认模拟履约：单短事务内按成交价结算买方扣款（先释放撮合时按买单限价预占的全量 `order_fulfillment` hold，再按 数量×成交价+order_fee 扣款，限价与成交价差额退回买方 available）、把库存以成交价为成本转入买方、返还卖方保证金、卖方收入到账（数量×成交价-order_fee），成交推进为 `fulfilled`，并追加 `p2p.trade.settled` 事实事件 + outbox + `market.reprice` 任务；响应 `{trade, balance}`。已 fulfilled/cancelled 返回 `409 RESOURCE_CONFLICT`，无关玩家返回 `404 RESOURCE_NOT_FOUND`（不泄露存在性）。
- `POST /v1/orders/trades/{tradeId}/cancel`（player，同上）取消模拟履约：退回买方全量待履约资金、`captureFunds` 扣除卖方已冻结保证金、`restorePartial` 恢复卖方库存（已成交数量加回 quantity/available），成交推进为 `cancelled`，**不产生 `p2p.trade.settled`**；写买卖各一条审计。
- `order.expire` 任务到期回收：创建委托与撮合成交时分别投递 `runAfter=expires_at`/`fulfillment_deadline`、`uniqueKey=order-expire:{id}`/`trade-expire:{id}` 的 `order.expire` 任务（`(type, unique_key)` 唯一索引去重）。到期把 `open/partially_filled` 委托转 `expired`（释放剩余资金/库存/保证金预占）或 `matched_pending_fulfillment` 成交转取消履约；`POST /v1/orders/trades/{tradeId}/expire`（admin）显式触发，普通玩家 `403`。状态机条件 UPDATE 保证已终态不重复迁移。履约期限沿用 `bilateral_order_limits.ttl_seconds`，由 `order-fulfillment/v1`（`resolveFulfillmentDeadline`）派生写入 `bilateral_trades.fulfillment_deadline`。迁移 `0021_p2p_fulfillment.sql` 追加该列与索引。
- 撮合（价格-时间优先、成交价、部分成交）由 `order-matching/v1` 决定；模拟履约/取消/到期在 `order-fulfillment/v1` 派生期限与状态校验；`p2p.trade.settled` 是结算事实，市场只消费、绝不由前端或 AI 触发；不引入实体物流状态。
- I21B 风控由 `order-risk/v1` 的纯函数校验价格边界、单笔/单日数量、下单冷却、窗口频率和潜在自买自卖。`bilateral_order_risk_limits` 为单例可配置输入；`order_risk_decisions` 是只追加决策流水。越界、频率、冷却和自买自卖在预占资产前以 `RULE_VIOLATION` 拦截；高频撤单只 `flagged` 供复核，仍按原事务释放资产。`GET /v1/admin/orders/risk-decisions` 仅 admin 可读，不提供放行或资产修改命令。

### I30B MTGJSON 系列/产品/补充包导入草稿（计划）

- 后端仅通过受控任务下载并校验 MTGJSON `SetList` 和管理员选择的单系列数据；系列、密封产品、booster 配置、卡表权重及其来源版本只能保存为草稿。草稿必须以本地 Scryfall 系列代码和 SKU 映射为准，缺失、歧义或不受支持工艺均不可发布。
- 发布命令要求管理员补齐虚拟币价格、启用范围和运营文案，并回传已预览的草稿/实体版本与 `Idempotency-Key`。PackModule 负责将通过校验的配置转换为不可变 `booster_pack_rules`；AdminModule 不得跨模块直接写补充包、目录、库存或价格表。导入、预览、发布、停用和失败均记录操作者、来源版本、请求 ID、幂等键与审计。

### I31B 备份、恢复、数据导出与部署（已完成，2026-07-31）

- `backup.create` 由持久化任务框架领取，在事务外用 better-sqlite3 `.backup()`（progress 回调返回 `remainingPages` 以正常推进）产出 WAL 一致副本；落盘后计算 SHA-256、做 `PRAGMA integrity_check`，写入只追加的 `backup_records`。失败只追加 `failed` 记录，**绝不删最近成功备份**。
- 每日以 UTC 自然日唯一键 `backup.create:daily:<date>` 投递一次（`backup_schedule_state` 单例，停机多日只补当天）；保留策略 `retentionToKeep` 保留至少 N 份 succeeded，超出部分清理磁盘文件，最新成功永不删。恢复演练 `restoreRehearsal` 在只读副本上校验完整性与核心表，绝不覆盖运行库；损坏文件按 `false` 返回而非抛错。
- 玩家导出由 `ExportService` 严格按 `request.actor.id` 过滤（用户隔离）：只读 `SqliteExportRepository` 覆盖库存/账本/NPC 交易/P2P 委托与成交/开包/比赛，写入 `EXPORT_DIR` 与 `export_records`（UNIQUE 幂等键、`expires_at`）。CSV 用 `export-rules`（`export/v1`）的 `toCsv` + `csvEscapeCell` 做公式注入防护（`= + - @` 前置单引号、分隔符按 RFC 4180 包裹）；列序冻结保证字段稳定。下载时服务端再次复核 `user_id` ownership 防越权。
- 部署：`docker-compose.yml` 三服务（`caddy` 唯一对外入口反代 `web`、`web` 反代 `/api` → `api`、`api` 容器内）。`Caddyfile` 用 `{$DOMAIN::80}` 支持有域名自动 HTTPS / 无域名纯 HTTP。`api-data` 命名卷持久化 SQLite、卡池、备份与导出。CI 推版本化镜像到 GHCR；`deploy.yml` 经 SSH 预备份（`wal_checkpoint` + 卷拷贝）→ 拉取重启 → 轮询 `/ready` → 失败保留预备份告警；回滚步骤见 `docs/deploy.md`。

### I32B 发布前后端质量门禁（已完成，2026-07-31）

I32B 不新增业务能力，把规则/经济一致性/安全/恢复/部署收敛为发布阻断条件。权威证据汇总见 `docs/release-acceptance-matrix.md`。

- 全局经济对账（`apps/api/src/tests/integration/economic-reconciliation.test.ts`，2 例）：把 I27B 闭环对账升级为全局只读恒等式清单——账户 `total = available + frozen` 且 `total = 账本净额`、`balance_after` 按账户/时间重放单调性、库存 `quantity = entry 净额` 且 `quantity = available + order_locked + tournament_locked`、冻结额守恒、库存锁定守恒、hold 引用闭包（成交终态时 hold 非 active）、事实事件完整性（`outbox` ↔ `fact_events`、`p2p.trade.settled` 仅 `fulfilled`）、奖励可追溯（赛事/成就 GAME_CREDIT 可经 `correlation_id` 反查账本）。终态闭环与活跃锁定中间态两场景；中间态覆盖全局表（含系统/NPC 侧）排除「终态巧合平衡」。任何一条不平即阻断发布。
- 服务端安全门禁（`apps/api/src/tests/integration/security-gate.test.ts`，9 例）：收敛分散在各模块的安全不变量为发布阻断级断言——Argon2id 密码哈希、错误/过期/无效令牌 401、CSRF 与 refresh 轮换重放拒绝、普通玩家访问任意 admin 路由 403 与未认证 401、认证频率限制、输入校验与统一错误包络、CORS 白名单、密钥隔离（生产环境拒绝示例 `AUTH_JWT_SECRET` 与默认 `DECK_RESPONSE_ENCRYPTION_KEY`）、`NEXT_PUBLIC_*` 公开配置不含服务端密钥、管理命令审计与日志脱敏。
- 需求追踪矩阵与 AT 自动化矩阵：`需求追踪表.md` 全部需求行更新为指向具体测试文件与 AT 编号的「已验证」证据，显式声明无「配置先于执行引擎」或「父验收早于子项」情况；AT-01 至 AT-13（AT-08 标注 I34）的自动化断言与浏览器/人工验收证据状态汇总于 `docs/release-acceptance-matrix.md`。人工验收用于验证真实浏览器操作、文案、视觉状态与可恢复性，不替代自动化断言（浏览器复跑属 I32F）。
- 运维门禁（`apps/api/docs/operations/README.md` I32B 节）：发布前门禁清单（`pnpm check` + 全量测试 + 经济对账 + 安全门禁 + I31B 恢复演练 + CI 构建 + 无 P0/P1）、首周观察指标表（任务失败率、价格快照新鲜度、账本不平告警、备份保留与完整性、市场报价有效性，附只读 SQL；AI 成本指标标注 I34 启用后纳入）、回滚流程（引用 `docs/deploy.md` 第 6 节）、值守与升级路径（P0/P1 分级、异常升级五步、禁止直接修数原则）。
- 验证：`pnpm --filter @mtg-market/api test` 全量 224 通过 + 1 跳过（含 I32B 新增经济对账 2、安全门禁 9）；`pnpm check`（lint + 全 workspace tsc）通过。`narrative.generate` 在 I33 发布前持续不投递/不领取。

### I34B 市场行情与交易体验服务端（已完成，2026-08-04）

首发后迭代，不改变 MVP 必须需求主责归属。把市场升级为可研究的行情屏，补齐批量交易、价格提醒与盘口聚合；涨跌统计、提醒触达与批量结算均以服务端为唯一权威。

- 只读行情统计 `GET /v1/market/heat`：日内/7 日涨跌幅榜与当日最活跃交易榜。涨跌幅按 `market_quotes` 自然日采样（复用 I17B 取样语义）由服务端整数计算 `changeBasisPoints`/`direction`，无历史基准返回 `flat`（`basePrice: null`）；`mostActive` 按当日已结算 `npc_trades` 与 `bilateral_trades(fulfilled)` 聚合张数与金额。浏览器不计算涨跌。
- 批量交易（D4）`POST /v1/npc-trades/sell/batch`：请求体 `{ skuIds: string[] }`（uuid、1–100、去重），逐 SKU 复用既有 NPC 卖出规则、报价快照与每日额度，卖出全部可用库存（不保留一张，区别于 C8 重复卡清仓）；账本+库存流水+成交+事实/outbox+重定价任务+审计在同一个短事务内提交，任一 SKU 结算失败整批回滚。跳过原因 `not_held/no_available_quantity/quote_unavailable/quote_stale/trade_limit_reached`；幂等键指纹依赖规范化 `skuIds`。
- 价格提醒/Watchlist（E12）：迁移 `0036` 追加 `watchlist_items`（每玩家每 SKU 去重、目标价/方向/启停、`watchlist_limits` 单例 50 上限）与 `watchlist_alerts`（`UNIQUE(user_id, watchlist_item_id, triggered_quote_id)` 收敛「同一报价至多一次提醒」）。`GET/POST/DELETE /v1/watchlist`、`GET /v1/watchlist/alerts`、`POST /v1/watchlist/alerts/{id}/read`；写操作要求幂等键与审计。`watchlist.check` 任务在每次 `market.reprice` 成功后投递（uniqueKey=`watchlist.check:reprice:{triggerKey}`），在同一事务内按最新报价快照执行 `hitWatchlistTarget`（`watchlist/v1` 纯规则，整数比较）写提醒；只读价格、不写经济表，任务失败走既有重试、绝不影响报价与市场。
- 订单簿盘口聚合：`GET /v1/orders/book/{skuId}` 扩展为按价格档聚合的买盘/卖盘深度——逐档 `cumulativeQuantity`（服务端累计），`midPrice`（最优买+最优卖 half-up）与 `spread`（最优卖−最优买），任一档缺失为 null；仍只读服务端聚合、不含用户身份。
- 系列周期与活动公告只读 `GET /v1/market/announcements`：只暴露生效中的 `market_series_cycles` 与 `market_events`（标题取 `admin_campaigns.name`，含影响范围、生效区间、关联系列），**绝不暴露 `factor_bps` 等内部系数与配置**；到期即不再返回。
- NPC 做市商倾向（E8/E14 增强）：`market_parameters` 新增可配置 `npc_bias_bps`（5000–20000，默认 10000 中性）与 `npc_bias_reason`，管理端 `POST /v1/admin/market-parameters` 可条件更新（字段可缺省，缺省沿用现值不重置）；reprice 时作为 `bias` 因素写入每个 SKU 的报价 reason，仍受既有市场系数上下限约束、不改外部参考价快照。
- 验证：`pnpm check` 通过；contracts 13、rules 85、database 4、api 245（1 跳过）、web 5 全过。新增测试见 `packages/contracts/src/index.test.ts`（公告不泄露系数/盘口扩展）、`packages/rules/src/index.test.ts`（bias 受界与 reason）、`apps/api/src/modules/market/application/market-service.test.ts`（热度/公告/bias）、`apps/api/src/modules/orders/api/npc-trade-batch.test.ts`（批量卖出幂等/跳过/回滚）、`apps/api/src/modules/orders/api/order-routes.test.ts`（盘口深度）、`apps/api/src/modules/watchlist/api/watchlist-routes.test.ts`（CRUD/去重/上限/提醒幂等/越权）与 `apps/api/src/tests/integration/admin-routes.test.ts`（倾向字段）。迁移号 0036。

### I35B 留存钩子与成长线服务端（已完成，2026-08-05）

首发后迭代，不改变 MVP 必须需求主责归属。用每日/每周任务与等级声望建立目标感；任务进度与经验只由服务端基于已结算事实推进（新手引导独立为 I36B/F）。

- 每日/每周任务系统（F3）：迁移 `0037` 固定 `task_definitions`（每日开包 3/交易 10/NPC 卖出 1/收藏价值 2000；每周参赛 3/任一系列完成度 80%）与 `task_instances`（`UNIQUE(user_id, definition_id, period_key)`，状态机 `pending→claimable→claimed`）。进度只由已结算事实（`pack.opened`/`npc.trade.settled`/`p2p.trade.settled`/`tournament.settled`）在同一 SQLite 短事务内同步推进（重放不重复计数；状态型 collection.value/set.completion 取样本峰值 max 不回退）；自然日/周重置以新 period_key 产生新实例，旧实例只读保留。奖励为**显式领取**：达标实例进入 `claimable`，`POST /v1/tasks/:instanceId/claim`（幂等键 + 条件 UPDATE 状态机）同事务入账 `task_reward` 账本 + `task.claimed` 审计；已领取/未达标/越权分别 `IDEMPOTENCY_CONFLICT`/`RULE_VIOLATION`/`RESOURCE_NOT_FOUND`。`GET /v1/tasks` 返回今日+本周实例（含 0 进度空态）与可领取数。
- 等级/声望系统（F5）：`level/v1` 纯规则——XP = 净资产每千分 20 + 已结算交易张数 5 + 80% 系列每系 10 + 100% 系列每系 50，等级阈值 1:0/2:200/3:500/4:1000/5:2000（封顶 5 级），称号随等级；`player_growth` 快照 total_xp/level 只升不降、净资产峰值只增不减。升级能力**服务层生效**：NPC 每日交易额度 `max_quantity_per_user_sku_day` 按 `npcDailyTradeMultiplier`（1/1/2/3/5）放大、批量开包上限 `bulkPackMax`（10/50/100/100/100）在 `POST /v1/packs/:packId/bulk` 强制校验（等级 1 与既有行为一致）；升级奖励（2:200/3:300/4:500/5:1000）在首次跨阈值时同事务发放（`level-up:{userId}:{level}` correlationId + 账本 + `level.up` 审计，持久化等级防重发）。`GET /v1/growth` 先按当前已结算事实刷新快照再返回档案；浏览器只展示。
- 无存档玩家读取任务/等级返回 409「请先创建游戏存档」。
- 验证：`pnpm check` 通过；contracts 14、rules 97、database 4、api 251（1 跳过）、web 5 全过。新增测试见 `packages/rules/src/daily-task-rules.test.ts`、`packages/rules/src/level-rules.test.ts`、`apps/api/src/modules/growth/api/growth-routes.test.ts`（任务状态机/周期重置/越权/等级奖励幂等）、`apps/api/src/modules/orders/api/npc-trade-batch.test.ts`（等级额度倍率）、`apps/api/src/modules/packs/api/pack-i33b-routes.test.ts`（等级批量开包门禁）与 `apps/api/src/tests/integration/economic-reconciliation.test.ts`（账本按写入序重放）。迁移号 0037。

### I36B 新手引导与首次体验服务端（已完成，2026-08-05）

首发后迭代，不改变 MVP 必须需求主责归属。把「工作资金 → 开包 → 看懂价格 → 首笔交易 → 收藏见涨 → 首次报名」的首次目标链做成服务端驱动的引导流程；步骤、进度与完成奖励只由服务端基于已结算事实推进与发放，浏览器只展示与提交意图，不判定完成、不结算任何经济真相。

- 引导步骤定义与版本化配置：`onboarding/v1` 纯规则固定六步——`claim-work-funds`（领取工作资金，`/dashboard`）、`open-first-pack`（开出第一包，`/packs`）、`view-price-history`（看懂价格，`/market/history`，view_event 步骤）、`complete-first-npc-trade`（完成首笔交易，`/market`）、`unlock-collection-album`（收藏见涨，`/collection/album`）、`first-tournament-registration`（首次报名，`/tournaments`），含说明文案、跳转路径、完成判定来源（`fact`/`profile`/`view_event`）与可跳过标志；配置版本化发布（迁移 `0038` 固定 `onboarding_steps` + `rule_versions` 的 `onboarding/v1` 行），进度行记录所依据步骤版本。
- 玩家引导进度持久化（幂等、可跳过、可重进）：`onboarding_progress` 以 `PRIMARY KEY(user_id, step_id)` 收敛并发/重放，`completed_at`/`skipped_at` 互斥。推进来源三类——`fact` 步骤由已结算事实（`pack.opened`/`npc.trade.settled`，首次目标链均目标 1）的同事务幂等消费者推进（与 I35B 任务共用同一已结算事实源，同一事实不重复计数）；`profile` 步骤由服务端对已结算状态快照判定（`daily_work_funding_claims`/`inventory_holdings`/`tournament_registrations`+`player_tournament_registrations`），满足即完成、只升不降；`view_event` 步骤由浏览器提交访问意图、服务端校验路径后写 `onboarding_events`（`UNIQUE(user_id, event_kind, step_id)` 防重复计数）置完成。跳过（`POST /v1/onboarding/steps/:stepId/skip`）永久视为已完成（老玩家补完路径），已完成后不可再跳过；全部写命令幂等键 + 条件校验，同键重放返回首次结果。
- 引导完成奖励：全部步骤完成后 `POST /v1/onboarding/reward/claim` 由服务端发放一次性固定奖励 500 GAME_CREDIT（`onboarding/v1` 规则定义），同事务写 `onboarding_reward` 账本（correlationId `onboarding-reward:{userId}`）+ `onboarding_reward_grants`（`PRIMARY KEY(user_id)` 防重发）+ `onboarding.reward.claimed` 审计；未全部完成 `RULE_VIOLATION`、已领取 `IDEMPOTENCY_CONFLICT`。`GET /v1/onboarding` 只读投影返回步骤、完成度、下一步与奖励状态（`unavailable`/`available`/`claimed`），未创建存档的新玩家也可只读与提交意图，领取奖励要求存档。
- 引导与首页联动：`GET /v1/dashboard` 在引导未完成或奖励未领取时追加待办 `continue_onboarding`（「继续新手引导」→ `/onboarding`），完成后消失；`PlayerDashboardDto.todos` 联合类型新增 `continue_onboarding`。
- 权威边界：步骤完成、跳过、奖励状态全部由服务端基于已结算事实/状态判定与持久化；引导不改变任何经济结算（不新增可结算规则，不触碰余额/库存/订单/赛事写路径），view_event 步骤只是提交访问意图。
- 验证：`pnpm check` 通过；contracts 17、rules 101、database 4、api 257（1 跳过）、web 5 全过。新增测试见 `packages/rules/src/onboarding-rules.test.ts`（步骤定义/推进/非法输入/路径匹配）与 `apps/api/src/modules/onboarding/api/onboarding-routes.test.ts`（事实推进/访问事件幂等/跳过与重入/奖励并发同键只入账一次/未完成与已领取拒绝/首页待办联动）。迁移号 0038。




### I05 调度语义

- 任务状态仅可按 `pending/failed → running → succeeded|failed|dead` 转换；`running` 必须带有限期租约。过期任务在启动补跑或领取前回收，已达最大尝试次数的任务转为 `dead`。
- 同一 `(type, unique_key)` 只保留一个任务事实；每一次领取以全局单调的运行序号写 `job_runs` 运行记录，错误摘要截断保存。失败任务使用有上限的指数退避；`failed` 和 `dead` 可通过管理接口人工重试，并重置调度尝试计数，但不重用历史运行记录序号。
- 处理器由任务注册表预先注册；I05 已注册 `catalog.sync`、`prices.sync`、`daily.rollover`、`market.reprice`、`tournament.settle`、`order.expire`、`backup.create`。`narrative.generate` 仅作为 I34 可选能力的预留类型，I33 发布前不得投递或领取。业务实现必须通过各自 application 用例并以自身事实唯一约束保证结果至多一次。

## 3.3 I24B 卡组草稿、规则与评分预备（已完成，2026-07-29）

- `DeckModule` 新增只读合法性与幂等草稿保存：仅 `commander-100/v1`，一或两名官方允许组合的指挥官、总计 100 张、颜色标识、Oracle 身份单例、Companion、版本化禁牌表和五种虚拟基本地均由 `packages/rules` 纯函数判定。草稿可非法或库存不足并保存以供编辑，但服务端返回明确问题；最终报名仍会在 I25B 重新验证。
- `card_printings` 持久化 Scryfall 的 `oracle_id`、`color_identity_json`、`type_line`、`keywords_json`、`mana_value`；`commander_banlist_versions` 固定当前官方版本。虚拟基本地只存为 `deck_cards.virtual_basic` 枚举，不引用 SKU、库存或市场资产。
- `decks`/`deck_cards` 与审计仅记录玩家草稿事实。创建/编辑不触及账户、`fund_holds`、`inventory_holds` 或比赛表，因此订单与比赛竞争库存的实际锁定仍严格延后到 I25B 报名短事务。
- Leyline 适配器只由未来报名 application 调用：它发送去标识化的 `format=commander` 和规范化 `decklistText`，受服务器端点/超时/重试/schema 控制。`deck_power_snapshots` 与 `deck_leyline_source_records` 已为 `leyline-only/v1` 预置只追加快照及 AES-GCM 密文留存；Provider 不可用、schema/网络失败不能产生评分或任何资产变化。

## 4. 不变量

1. 不允许负余额、负库存或同一张库存卡同时被订单与已报名比赛使用；基本地是不可交易的虚拟资源，不进入库存或锁定账。
2. 无外部价、零价、映射不完整或同步异常 SKU 暂停新增交易；已有库存按最后成功快照估值。
3. 取消模拟履约的保证金扣款、订单状态、库存/买方资金恢复和流水必须同一事务、可追溯。
4. 外部快照、开包记录、赛果、奖励与审计日志只追加，不被后续任务覆盖。
5. 密码仅保存 Argon2id 哈希；access token 必须短期有效，refresh token 仅以哈希持久化并可撤销。Cookie 型刷新与注销必须校验 CSRF，admin 路由必须在服务端复核角色。
6. 管理员修正余额或库存必须调用所属模块的补偿命令，在同一短事务写新流水、原因、操作者、幂等键和关联原记录；禁止直接覆盖最终值、删除或改写历史流水。
7. 已发布活动不可原地改写；发布、暂停和结束采用实体版本与幂等键，活动的时间、作用范围和影响参数必须经服务端规则校验，成功后以活动版本唯一键投递可重放的 `market.reprice`。
8. 审计与运行日志不提供删除或通用修改接口；查询必须服务端分页、鉴权和脱敏，不返回密码哈希、令牌摘要、Cookie、密钥或未处理的 Provider 敏感输入。
9. 报名命令才调用候选评分 Provider 并持久化强度快照：服务器提交规范化卡表和 `format=commander`，Leyline 来源保存完整服务器专用响应、`scores.power`、其余分项、Bracket、响应 SHA-256、`computedAt`、解析/缺失卡、`isStale` 与本地适配器版本。ADR-003 规定首发以 `leyline-only/v1` 积累去标识化训练数据；达到 `ml-eligibility/v1` 门槛并经管理员发布后，ML 可成为新报名主源。规则标签模型只输出版本化、可解释的卡表特征和特征分：土地/曲线、抓牌、加速、去除、反制、导师、快速法术力、无限/致胜组合、墓地/神器/结界交互、核心组件冗余、获胜路线和 Commander Game Changer/禁牌/Bracket 条件；模拟与 ML 校准最终分数，禁牌和赛制条件始终硬拒绝。快照一律标明评分来源、版本、输入摘要和降级原因（适用时）。Provider 未返回算法版本时必须明确标为未声明；结算仅消费报名时已持久化快照、本地差值规则与种子，评分来源均不可用时不得改变既有赛事结果，并且必须在收费、锁卡或创建报名前失败。
10. 成就仅能由 `achievement.process` 幂等消费已结算的 `tournament.settled` fact。成就规则、进度与风控判定必须来自版本化纯规则；解锁、`GAME_CREDIT`/SKU/徽章奖励、奖励状态、每日奖励/重复参赛计数与审计必须同一短事务完成。`achievement_unlocks(user_id,definition_id)` 与 `achievement_reward_grants(user_id,definition_id)` 是历史补跑和并发处理下的最终去重边界。

## 5. 管理后台服务端能力（I30B，已完成 2026-07-31）

- 所有管理读写路由位于 `/v1/admin/*`，由 `requireRole("admin")` 在服务端复核角色；前端路由保护不能替代此检查。管理写请求要求格式正确的 `Idempotency-Key`、必填原因（适用时）、实体版本与不可变 `audit_logs` 审计。
- `AdminModule` 四层实现（api/application/domain/infrastructure）只通过 users/inventory/packs/market/catalog/jobs 各模块 application 端口协作，绝不跨模块直写 users/accounts/inventory 等他模块表（用户冻结、会话撤销除外，仅改 users/sessions 行）。
- 后台首页聚合：`GET /v1/admin/dashboard` 返回环境、目录/价格新鲜度、失败任务、活动数、待复核异常与最近操作摘要。
- 日志查询：`GET /v1/admin/audit-logs` 只读、服务端分页，支持按时间、操作者、用户、实体、动作、请求 ID、任务类型筛选，不提供删除/修改或敏感原文；`GET /v1/admin/audit-logs/{id}` 返回脱敏详情与有限关联记录（不递归无限展开）。`GET /v1/admin/exception-trades` 聚合 flagged 风控决策与失败/死亡任务。
- 活动状态机：`admin_campaigns`（draft/previewing/published/paused/ended）与不可变 `admin_campaign_versions` 快照。`POST /v1/admin/campaigns` 保存草稿，`POST .../{id}/preview` 推进版本并返回冲突与预计任务，`POST .../{id}/publish` 绑定预览版本+实体版本+幂等键发布，`POST .../{id}/schedule` 以 `starts_at` 投递定时重价任务，`POST .../{id}/pause|end` 暂停/结束。发布写入只追加的 `market_events`，以活动版本唯一键投递 `market.reprice`，事件不改写外部价格快照；同 scope+区间重叠的已发布活动互斥。
- 用户管理：`GET /v1/admin/users` 按 ID/用户名/角色/状态分页，`GET /v1/admin/users/{id}` 返回脱敏详情（不含密码哈希/令牌）。冻结/解冻写 `users.frozen_at/frozen_reason`（迁移 `0030`）；`POST .../revoke-sessions` 撤销该用户未撤销会话；余额/库存补偿经 users/inventory application 端口追加 `admin_compensation` 账本/库存流水，禁止直接覆盖最终值或删除原流水，余额/库存不足时不留半完成状态。
- 配置：`GET/POST /v1/admin/market-parameters` 单例参数版本预览（迁移 `0031` 新增自增 `version` 列）与条件更新；`POST /v1/admin/catalog/skus/{id}/tradable` 切换可交易资格，不改目录或价格快照；`POST /v1/admin/catalog/sync-trigger|/v1/admin/prices/sync-trigger` 以 uniqueKey 去重投递同步任务；`POST /v1/admin/jobs/{id}/retry` 重试任务。
- MTGJSON 导入草稿：`mtgjson_import_drafts` 表保存来源版本、checksum 与映射摘要；`POST /v1/admin/mtgjson/setlist-draft` 创建 SetList 草稿（`UNIQUE(draft_kind, set_code, source_version)` 去重），`POST .../drafts/{id}/preview` 按本地 Scryfall 系列代码映射展示可导入/缺失/冲突，`POST .../drafts/{id}/discard` 丢弃。绝不直接改写目录、库存、价格快照或已发布补充包。
- 补充包规则发布：`POST /v1/admin/packs/{packId}/rule-preview` 服务端候选池/卡位/权重/工艺校验与版本预览，`POST .../rule-publish` 经校验后以 `(pack_id, version)` 不可变快照追加并切换 `active_rule_version`（不可原地覆盖，重复版本返回冲突），`POST .../disable` 停用保留审计。
- 待决：人工例外来源创建命令、比赛/成就/奖励配置编辑路由延后至 I30F 或后续迭代，首发不展示这些尚未开放的配置入口；I33 发布后若启用 AI，才追加 Agent 记录查询。
- 验证：`apps/api/src/tests/integration/admin-routes.test.ts`（14 例）+ `apps/api/src/modules/admin/domain/admin-domain.test.ts`（14 例）覆盖普通玩家 403、会话撤销、同键重放/异参、并发版本冲突、活动越界/冲突/重复发布、暂停/结束重放、用户补偿事务回滚、MTGJSON 草稿映射/去重、同步重复触发、补充包规则不可变发布、市场参数版本冲突、日志只读与敏感字段脱敏。

## 5.1 历史 I30B 计划要求（已被上方实现取代）
