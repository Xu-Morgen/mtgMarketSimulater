# 后端需求

本文件定义服务端的经济真相、事务和异步任务。完整技术栈见[技术栈与模块职责边界.md](技术栈与模块职责边界.md)。

## 1. 技术与运行要求

- Fastify + OpenAPI；SQLite（WAL 模式）+ Drizzle ORM；SQLite `jobs` 表与进程内任务循环；Docker Compose 部署。HTTP 层统一返回 contracts 响应包络、回传请求 ID、使用结构化 Pino 脱敏日志，并仅允许配置的 CORS Origin。
- 所有余额、库存、订单、保证金、开包、比赛奖励与成就写操作仅由服务端完成。
- 关键结算使用 SQLite 短事务、唯一约束、条件更新和 idempotency key；所有经济变动写追加式审计流水。单实例串行处理结算，避免 SQLite 写竞争。
- 数据库 schema 与 SQL 迁移统一由 `packages/database` 管理；启动先执行版本化迁移与 `PRAGMA integrity_check`。每个迁移必须短事务原子提交，失败时不写该迁移记录且保留迁移前状态。

## 2. 业务模块

| 模块 | 必须职责 |
| --- | --- |
| 用户与认证 | 账户、会话、角色、状态、初始资金与每日工作资金；以用户+自然日唯一约束防重复发钱；为管理员提供用户检索、冻结/解冻、会话撤销和只读经济摘要。 |
| 卡牌目录 | 后端同步 Scryfall 卡牌与印刷版本，生成可管理 SKU。 |
| 外部价格与 MTGJSON 导入 | 导入 MTGJSON、校验映射、只追加价格快照；`AllPrices` 历史回填只补齐缺失日期。系列、密封产品与 booster 资料仅作为管理员审核的导入草稿，异常时保留最后成功版本。 |
| 市场与 NPC | 消费已结算的开包/NPC/P2P 事实事件，使用规则引擎计算参考价、供需聚合、系列/关联/事件系数、NPC 买卖价、价差与限额。 |
| 库存与开包 | 管理可用/锁定库存；开包、扣款、库存变化在同一事务完成。 |

## 2.1 I07B 存档、账户与账本（已完成，2026-07-24）

- `POST /v1/archive` 在用户、存档、账户、初始 `initial_funding` 流水、业务审计与幂等完成记录之间采用单个 SQLite 短事务；`game_archives.user_id`、账户币种与 `(actor_id, idempotency_key)` 唯一约束共同防止重复发放。
- 初始资金规则为 `initial-funds/v1`，从 `@mtg-market/rules` 解析，定义同步存入 `rule_versions`；每份存档保存使用的规则版本。余额只以 `GAME_CREDIT` 整数最小单位保存，数据库强制总额等于可用额加冻结额且不为负。
- users application 提供 reserve/release/capture 共享资金原语。冻结记录必须关联业务实体和原因；扣除必须由 active hold 发生并新增 debit 账本。任何未来订单或履约操作不得直接更新账户字段。

## 2.2 I10B 库存、成本与资产锁定（已完成，2026-07-24）

- `inventory_holdings` 以 `(user_id, sku_id)` 唯一记录持有量、可用量、订单锁定量、比赛锁定量、整数移动平均成本和可空市值快照；数据库约束强制持有量等于三类数量之和且全部非负。
- `inventory_holds` 保存订单/比赛的 active、released、captured 状态及关联实体，`inventory_entries` 为不可变数量与成本流水。锁定使用条件更新；释放或 capture 仅可消费 active hold，因此重放不会超额解锁或产生幽灵库存。
- `InventoryService.withLedgerTransaction` 明确要求开包、订单和比赛将库存、账本、事实事件及审计置于同一 SQLite 短事务。玩家 API 只提供当前用户的库存总览、单卡持仓与可分页对账查询；不提供直接改库存、成本或锁定量的接口。

## 2.3 I11B 补充包配置与可重放规则（已完成，2026-07-26）

- `booster_packs` 保存补充包商品价格、启用状态、停用原因与当前规则版本；`booster_pack_rules` 以 `(pack_id, version)` 保存不可变的完整卡位、候选池、SKU 整数权重定义。规则包负责校验边界、计算概率及以明确种子生成确定性结果；MVP 没有任何保底、计数器或跨包状态字段。
- `GET /v1/packs` 与详情路由只公示服务端计算的稀有度概率、价格、启用状态和规则版本，绝不交付候选池、种子或开奖结果。配置写入/发布管理命令仍归 I30B 管理模块，I11B 不提供浏览器开奖命令。
- `PackService` 只在服务端用 CSPRNG 生成 32-byte 种子，并将原始种子、SHA-256 与规则结果摘要追加到无 HTTP 读取路径的 `pack_rule_replays`。I12B 必须在既有库存/账本共同事务中调用此能力，不能将种子、结果、扣款或库存拆分提交。
- 迁移 `0013_base_bro_sos_packs.sql` 创建 `BRO-BASE`、`SOS-BASE` 两款 500 游戏币基础补充包，初始停用以等待目录。成功的 `catalog.sync` 在替换 Scryfall 目录的同一短事务内调用 packs application，为当前系列的非闪 `common`/`uncommon`/`rare`/`mythic` SKU 追加新规则快照：10 普通、3 非普通、1 张按 7:1 稀有/秘稀选择的卡位；缺少必要稀有度时保持停用。同步重跑只追加并切换版本，绝不覆盖旧规则或已结算记录。

## 2.4 I12B 商店购买与服务端开包（已完成，2026-07-26）

- `GET /v1/store/packs`、购买预览、`POST /v1/packs/{packId}/open` 与当前玩家的开包历史共同构成商店协议。购买请求回传服务端预览中的规则版本并携带幂等键；下架、失效 SKU、版本过期、未建档或余额不足均在扣款前拒绝。
- `pack_openings` 与 `pack_rule_replays` 一对一关联，追加规则版本、消费额和不含种子的结果摘要。开包用例使用 inventory application 事务编排，同时追加 `pack_purchase` 账本、`pack_opened` 库存流水、`pack.opened` 事实事件/outbox 与业务审计；故障不能留下任何半完成记录。
- I17B 价格/市场模块上线前，结果 DTO 的参考价、游戏内价和盈亏都返回 `null`，并以 `unavailable_until_i17` 说明状态；事实事件只供后续消费，不在本期生成市场报价。
| P2P 订单 | 双边委托确认、资金/库存锁定、撮合、模拟履约期限、保证金、取消与风控。 |
| 卡组与比赛 | 校验库存与赛制、锁定参赛卡牌、确定性赛果、奖励、成就与重放信息。 |
| 管理与审计 | 管理员仪表盘聚合；活动草稿、预览、发布/定时发布、暂停和结束；用户管理命令；参数版本化；只读、分页、脱敏的后台/经济/任务/异常交易/Agent 日志查询。 |

## 3. 异步任务

- `catalog.sync`：使用 Scryfall Bulk Data 同步卡池，按需写入本地卡图缓存；失败不删除现有目录或图片。
- `prices.sync`：每日同步 MTGJSON；成功后以快照唯一键投递 `market.reprice`，失败继续使用最近成功快照并告警。I17B 增加独立的一次性 `AllPrices` 历史回填意图，只补齐缺失日期，不替代或重写日常同步。
- `daily.rollover`：按服务器自然日开放工作资金和刷新比赛，必须幂等。
- `market.reprice`：根据价格快照、供需事件、系列周期、关联卡、流动性与运营事件生成有界游戏报价；失败保留最近成功报价。
- `tournament.settle`：结算赛果、奖励与成就，成功后才投递 AI 战报任务。
- `narrative.generate`：调用 AI 或生成模板战报；绝不回滚比赛结果。
- `order.expire`：处理过期委托、模拟履约时限和保证金。
- `backup.create`：生成并校验 SQLite 一致性备份，执行保留策略；失败不得删除最近成功备份。

### I09B 目录同步实现（已完成，2026-07-24）

- `catalog.sync` 仅由服务端任务处理器调用 Scryfall Bulk Data。它使用服务端 `SCRYFALL_USER_AGENT`（不得使用 Node 默认值）请求元数据、Bulk 文件和卡图，兼容 gzip 编码，并按对象扫描完整数组、只保留启用系列，避免完整 Bulk 文件超出 Node 字符串上限；它记录来源版本、下载 URI、SHA-256、启用系列、导入差异、运行时间和失败摘要；`catalog_sync_state` 永远指向最近一次成功运行。
- 导入先验证下载 JSON、必要印刷字段、Scryfall UUID、支持的工艺和重复印刷；随后在单个 SQLite 短事务内替换 `scryfall` 来源的系列、印刷、SKU 和图片元数据。任一失败不删除上次成功目录或状态指针。
- 仅 payload 中明确指定的卡图会下载到 `CATALOG_DATA_DIR/images`；文件名受 UUID/扩展名白名单约束，玩家只经认证后的本地静态路由读取，浏览器不访问 Scryfall。
- `/v1/admin/catalog/sync` 的读写都要求 `admin` 角色；写入要求 `Idempotency-Key` 并以任务唯一键重放同一次投递。固定夹具覆盖 checksum、截断、重复、缺字段、非法图片路径和事务中断。

### I13B MTGJSON 价格快照实现（已完成，2026-07-26）

- `prices.sync` 仅由服务端任务调用 `AllPricesToday` 和 `AllPrintings` 及各自 `.sha256` 侧车文件，记录两份文件的版本、SHA-256、校验状态与脱敏失败摘要。AllPrintings 解压后逐张解析 `cards` 对象，不得将整份文件转换为单一 JavaScript 字符串。其 `identifiers.scryfallId`、卡片 UUID 和 `nonfoil`/`foil`/`etched` 工艺共同映射到本地 SKU；只采集 Cardmarket `EUR` retail 中相应价格类型的最新日期正值并换算为整数欧分。
- 校验或写入失败时，`PriceSyncService` 通过任务进程的结构化日志输出同步批次、任务 ID/尝试次数、来源版本、校验阶段、关联文件及预期/实际 SHA-256；日志不得包含下载 URL、Provider 原始响应、密钥或 Cookie。`price_sync_runs` 继续只保存可向管理员展示的脱敏失败摘要。
- `price_sync_runs`、`price_sku_mappings` 与 `price_snapshot_entries` 均只追加；成功导入在一笔短事务内追加每个 Scryfall SKU 的 `priced`、`no_price` 或 `mapping_failed` 状态并更新 `card_skus.tradable`，随后才移动 `price_sync_state`。零价、无价、币种不符、缺失/歧义映射均暂停新增交易；下载、checksum、解析或事务失败不会替换最近成功快照。
- `/v1/admin/prices/sync` 的读写均要求 `admin`；POST 需要 `Idempotency-Key`，可用两份 expected SHA-256 固定受控导入。仅当最近运行的稳定失败码为 `CHECKSUM_MISMATCH` 时才允许 `allowChecksumMismatch: true` 覆写；该请求与成功快照分别留下专门审计事实和 `bypassed` 校验状态。固定夹具覆盖 normal/foil/etched、EUR、日期、零价/缺失、重复映射、checksum、覆写与事务中断；成功后以同步运行 ID 投递 I14B 的唯一 `market.reprice`，仍不改写库存估值。
- I13F 补充只读的 `/v1/prices/status`：任意已认证玩家只能读取来源、最近成功更新时间和服务端新鲜度枚举；当前同步失败而保留旧成功快照时标为 `stale`，无成功快照时标为 `unavailable`。版本、校验和、统计、任务和失败原因仍禁止离开 admin API。

### I14B 市场规则、报价与 NPC 报价（已完成，2026-07-27）

- `@mtg-market/rules` 的 `market/v1` 将 Cardmarket EUR 欧分经整数 bp 兑换为 `GAME_CREDIT`，以明确 half-up 舍入计算有界游戏内中间价、NPC 买入/卖出价和费用；规则拒绝浮点、未知版本、无效最低价/价差/费用及越界系数，并保留每项计算原因。
- `MarketService` 消费 `pack.opened`、`npc.trade.settled`、`p2p.trade.settled` 与 `tournament.settled` 的已结算事实（当前开包/NPC/P2P 产生供需和流动性压力），再组合系列周期、SKU 关联和仍在 UTC 生效期内的 scoped 市场事件。它只追加 `market_quotes` 投影，绝不回写 `price_snapshot_entries`、库存、余额或账本。
- 迁移 `0016_market_quotes.sql` 保存版本化参数、系列周期、关联、带作用范围/生效区间/原因/5,000–20,000 bp 上限的事件，以及含输入参数、原因、外部快照和规则版本的报价。`(sku_id, trigger_key)` 唯一约束保证价格同步或事实事件重放不会重复叠加。
- 成功 `prices.sync` 以 `price-sync:<runId>`、成功 `pack.opened` 以 `fact-event:<eventId>` 投递重定价；任务失败时保留最近成功报价并按 jobs 重试。`GET /v1/market/quotes/{skuId}` 与 `/v1/market/index` 仅向认证用户提供持久化结果；无有效报价返回 `PRICE_UNAVAILABLE`。新交易资格仍由价格同步的 `card_skus.tradable` 决定，旧持仓不因报价投影失败被删除。

### I15B NPC 买入结算（已完成，2026-07-27）

- `GET /v1/npc-trades/buy/{skuId}/preview` 按数量返回服务端报价快照 ID、规则版本、内含费用的单位/总价、有效期和服务器交易额度；`POST /v1/npc-trades/buy/{skuId}` 只接受该报价标识、版本、数量与玩家确认的最高单位价，金额与费用绝不来自浏览器。
- 迁移 `0017_npc_trades.sql` 创建只追加的 `npc_trades` 与受控 `npc_trade_limits`。每笔成交固定引用 `market_quotes.id`、规则版本、价格、费用、总额和 UTC 结算日；单笔及用户/SKU/日额度均由服务器检查，日额度以已结算记录聚合。
- `market/v1` 以报价计算时间生成固定 15 分钟有效期，不将日外部快照的采集时刻误作交易有效截止；`NpcTradeService` 在一个短 SQLite 事务内通过 market/users/inventory application 接口完成报价校验、`npc_buy` 账本、库存增加与成本、成交记录、`npc.trade.settled` 事实/outbox、唯一重定价任务、审计和幂等响应。报价不可用、过期/限价或版本不符、余额不足、额度超限及库存写入异常不会留下半完成结算。

### I17B MTGJSON `AllPrices` 历史回填（计划）

- 管理员提交独立幂等意图后，后端下载、SHA-256 校验并按流式/受限内存方式解析 `AllPrices`。回填以本地 Scryfall SKU、MTGJSON UUID、工艺和快照日期为唯一映射，只追加当前数据库缺少的外部价格日期；不得覆盖每日同步写入的历史、改变 `price_sync_state` 最近成功指针，或向每一个历史日期投递 `market.reprice`。
- 回填运行必须记录来源版本、checksum、日期范围、插入/跳过/无价/映射失败统计、操作者、请求 ID 与幂等键。解析、校验、映射或事务失败时不得留下半批次条目，已存在价格快照与日常同步保持可用。

### I30B MTGJSON 系列/产品/补充包导入草稿（计划）

- 后端仅通过受控任务下载并校验 MTGJSON `SetList` 和管理员选择的单系列数据；系列、密封产品、booster 配置、卡表权重及其来源版本只能保存为草稿。草稿必须以本地 Scryfall 系列代码和 SKU 映射为准，缺失、歧义或不受支持工艺均不可发布。
- 发布命令要求管理员补齐虚拟币价格、启用范围和运营文案，并回传已预览的草稿/实体版本与 `Idempotency-Key`。PackModule 负责将通过校验的配置转换为不可变 `booster_pack_rules`；AdminModule 不得跨模块直接写补充包、目录、库存或价格表。导入、预览、发布、停用和失败均记录操作者、来源版本、请求 ID、幂等键与审计。

### I05 调度语义

- 任务状态仅可按 `pending/failed → running → succeeded|failed|dead` 转换；`running` 必须带有限期租约。过期任务在启动补跑或领取前回收，已达最大尝试次数的任务转为 `dead`。
- 同一 `(type, unique_key)` 只保留一个任务事实；每一次领取以全局单调的运行序号写 `job_runs` 运行记录，错误摘要截断保存。失败任务使用有上限的指数退避；`failed` 和 `dead` 可通过管理接口人工重试，并重置调度尝试计数，但不重用历史运行记录序号。
- 处理器由任务注册表预先注册；I05 固定预注册 `catalog.sync`、`prices.sync`、`daily.rollover`、`market.reprice`、`tournament.settle`、`order.expire`、`narrative.generate`、`backup.create`。业务实现必须通过各自 application 用例并以自身事实唯一约束保证结果至多一次。

## 4. 不变量

1. 不允许负余额、负库存或同一张卡同时被订单与比赛使用。
2. 无外部价、零价、映射不完整或同步异常 SKU 暂停新增交易；已有库存按最后成功快照估值。
3. 取消模拟履约的保证金扣款、订单状态、库存/买方资金恢复和流水必须同一事务、可追溯。
4. 外部快照、开包记录、赛果、奖励与审计日志只追加，不被后续任务覆盖。
5. 密码仅保存 Argon2id 哈希；access token 必须短期有效，refresh token 仅以哈希持久化并可撤销。Cookie 型刷新与注销必须校验 CSRF，admin 路由必须在服务端复核角色。
6. 管理员修正余额或库存必须调用所属模块的补偿命令，在同一短事务写新流水、原因、操作者、幂等键和关联原记录；禁止直接覆盖最终值、删除或改写历史流水。
7. 已发布活动不可原地改写；发布、暂停和结束采用实体版本与幂等键，活动的时间、作用范围和影响参数必须经服务端规则校验，成功后以活动版本唯一键投递可重放的 `market.reprice`。
8. 审计与运行日志不提供删除或通用修改接口；查询必须服务端分页、鉴权和脱敏，不返回密码哈希、令牌摘要、Cookie、密钥或未处理的 Provider 敏感输入。

## 5. 管理后台服务端能力（I30B）

- 所有管理读写路由位于 `/v1/admin/*`，先验证有效会话再要求 `admin` 角色；前端路由保护不能替代此检查。管理写请求要求 `Idempotency-Key`，并记录请求 ID、操作者、对象、版本、原因和结果摘要。
- 活动 API 区分草稿保存、发布预览、发布/定时发布、暂停和结束命令。预览返回可确认的版本、目标范围、UTC 生效区间、参数上限/冲突和预计任务；提交时版本不一致必须返回稳定冲突错误。
- 用户查询按用户 ID、用户名、角色和状态服务端分页；详情只返回完成管理任务所需的账户/会话状态与余额、账本、库存、订单、比赛、审计摘要。冻结/解冻、会话撤销和补偿修正分别使用显式命令，不提供跨表通用 CRUD。
- 日志查询按时间、操作者、用户、实体、动作、结果、请求 ID、任务类型分页筛选，并返回脱敏详情和关联标识。管理页面查看日志本身可记录访问审计，但不得制造递归的无限日志链。
- Fastify inject + 临时 SQLite 集成测试覆盖普通玩家 403、会话撤销、重复管理命令、同键异参、活动版本冲突/参数越界/重复发布、补偿事务回滚、日志不可修改和敏感字段脱敏。
