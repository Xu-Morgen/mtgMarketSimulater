# 部署速查（傻瓜版，照抄即可）

> 只会复制粘贴命令就能把项目跑起来。把下面的命令**一行一行**粘到服务器终端里执行。
> 适用于：一台刚装好的 Ubuntu（22.04 或 24.04）服务器，用 root 或有 sudo 的账号登录。

---

## 第 0 步：登录服务器

用你自己的方式连上服务器（云厂商控制台、SSH 客户端都行）。连上后继续往下做。

---

## 第 1 步：装 Docker（只装一次）

```bash
curl -fsSL https://get.docker.com | sh
```

等它跑完。验证装好了：

```bash
docker compose version
```

看到类似 `Docker Compose version v2.x.x` 就成功。

---

## 第 2 步：把项目代码拉到服务器

```bash
cd /opt
git clone https://github.com/Xu-Morgen/mtgMarketSimulater.git
cd mtgMarketSimulater
```

---

## 第 3 步：生成两个密钥（复制下面的两行，各执行一次）

```bash
openssl rand -base64 32
```

把**输出的那串字符**记下来（比如 `Ab3xK9...==`），这是 **密钥 A**。

```bash
openssl rand -base64 32
```

再记下另一串，这是 **密钥 B**。

> 两串都要保留，下一步要用。别用示例值，项目会拒绝启动。

---

## 第 4 步：填写配置文件

复制示例文件：

```bash
cp .env.docker.example .env.docker
```

用编辑器打开（任选其一）：

```bash
nano .env.docker
```

找到下面这几行，**改成你自己的值**：

```ini
# 改成你的服务器访问地址。没域名就填 http://服务器IP:3000
WEB_ORIGIN=http://你的服务器IP:3000
CORS_ORIGINS=http://你的服务器IP:3000

# 粘贴第 3 步的【密钥 A】（去掉前面的井号，直接贴在等号后面）
AUTH_JWT_SECRET=粘贴密钥A
# 粘贴第 3 步的【密钥 B】
DECK_RESPONSE_ENCRYPTION_KEY=粘贴密钥B

# 改成你的联系邮箱（外部数据源会校验，不改可能被限流）
SCRYFALL_USER_AGENT=MTG-Market-Simulator/0.1 (contact: 你的邮箱)
MTGJSON_USER_AGENT=MTG-Market-Simulator/0.1 (contact: 你的邮箱)
```

改完保存退出（nano 是 `Ctrl+O` 回车 `Ctrl+X`）。

> 想换端口？把 `WEB_PORT=3000` 改成别的，比如 `8080`。

---

## 第 5 步：构建并启动（第一次会等几分钟）

```bash
docker compose build
```

```bash
docker compose up -d
```

看到 `Started`、`Running` 之类的字样就成功了。

---

## 第 6 步：验证（确认真的跑起来了）

等 20 秒让它启动，然后：

```bash
docker compose ps
```

`api` 和 `web` 两行都应是 `running` / `healthy`。

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/
```

看到 `200` 就对了。

浏览器打开（把 IP 换成你的服务器 IP）：

```
http://你的服务器IP:3000
```

能正常显示页面 = 部署成功。

---

## ✅ 到这里就部署完成了。

---

# 日常运维（需要时再查）

## 看实时日志

```bash
docker compose logs -f
```

只看 API：`docker compose logs -f api` ｜ 只看 Web：`docker compose logs -f web`
退出按 `Ctrl+C`。

## 重启服务

```bash
docker compose restart
```

## 升级到新版本（拉新代码后）

```bash
cd /opt/mtgMarketSimulater
git pull
docker compose build
docker compose up -d
```

数据不会丢（存在持久卷里）。

## 备份数据（重要！定期做）

```bash
cd /opt/mtgMarketSimulater
docker compose exec -T api node -e "const d=require('better-sqlite3')('/app/data/market-simulator.db'); d.pragma('wal_checkpoint(TRUNCATE)'); d.close();"
docker cp "$(docker compose ps -q api)":/app/data ./backup-$(date +%Y%m%d)
```

备份会存到 `./backup-年月日` 文件夹。把它复制到别处更安全。

## 停止服务

```bash
docker compose down
```

---

# 常见问题

**页面打不开 / 浏览器一直转：**
1. 确认服务器防火墙 / 安全组放行了 `3000` 端口（云厂商控制台设置）。
2. 确认 `WEB_ORIGIN` 和 `CORS_ORIGINS` 填对了 IP 和端口。
3. 看日志：`docker compose logs -f`。

**启动失败、提示密钥错误：**
`AUTH_JWT_SECRET` 和 `DECK_RESPONSE_ENCRYPTION_KEY` 必须是第 3 步用 `openssl rand` 生成的随机值，不能用示例值或留空。改完 `docker compose up -d` 重来。

**端口 3000 被占用：**
在 `.env.docker` 把 `WEB_PORT=3000` 改成 `8080`（或其他空闲端口），再 `docker compose up -d`。

**想用 HTTPS（有自己的域名）：**
本项目本身只跑 HTTP。在前面加一层 Nginx/Caddy 反代把 443 转到 3000 即可。`WEB_ORIGIN` 改成 `https://你的域名`。

---

> 更完整的说明（含服务器配置建议、手动同步、HTTPS 细节）见 `docs/deploy.md`。
