用户已批准该计划（"批准，使用代理链接，增加CI的docker-build验证job"）。明确两点确认：
1. **前端访问 API 采用代理方式**：Next.js rewrites 把 `/api/*` 代理到 api 服务，省 CORS、更安全。
2. **CI 增加 docker-build 验证 job**：除构建+测试 job 外，额外加一个只构建不推送的 docker job。

以下为完整实施计划，现在开始执行。

## 第一部分:修复生产构建阻塞
1. `packages/contracts/tsconfig.json`、`packages/rules/tsconfig.json`:加 `rootDir: src`、`outDir: dist`、`declaration: true`,并 `exclude: src/**/*.test.ts`(避免测试文件进 dist)。
2. `packages/contracts/package.json`、`packages/rules/package.json`:`exports` 由 `./src/index.ts` 改为 `{"types":"./dist/index.d.ts","default":"./dist/index.js"}`;`build` 由 `tsc --noEmit` 改为 `tsc -p tsconfig.json`。
3. 验证:`pnpm -r build` 产出 dist;`apps/api start` 通过 `/health`。

## 第二部分:前端代理
4. `apps/web/next.config.mjs`:加 `rewrites()` 把 `/api/:path*` 转发到 `${process.env.API_PROXY_TARGET ?? 'http://localhost:3001'}/:path*`;默认 `NEXT_PUBLIC_API_BASE_URL` 改为 `/api`。

## 第三部分:Docker 化
5. 根目录 `Dockerfile`(多阶段:deps 装原生编译工具链 → build 全量构建 → runner 只带产物)。
6. 根目录 `.dockerignore`:排除 node_modules/.next/dist/*.db/.env/.git 等。
7. 根目录 `docker-compose.yml`:api(挂 SQLite 卷 + healthcheck)、web(依赖 api healthy)双服务。
8. 根目录 `.env.docker.example`:列出注入项,敏感项留空并注释"务必替换"。

## 第四部分:CI
9. `.github/workflows/ci.yml`:job1 install→lint→check→build→test;job2 docker-build(只构建不推送)。

## 第五部分:文档
10. `docs/deploy.md`:服务器选型、初始化、密钥生成、构建启动、验证、运维、安全。

## 验证
- `pnpm -r build` + `pnpm check` + `pnpm test` 全绿
- `docker compose build` + `up -d`,Web 显示"API 正常"