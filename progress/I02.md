# I02 共享契约、错误模型与幂等协议

完成日期：2026-07-24

## 新增

- `packages/contracts/src/index.test.ts`：覆盖稳定序列化、非法 JSON 值、请求 ID/幂等键/请求指纹、整数金额与冲突响应、事实事件的契约测试。
- `需求追踪表.md`：提供“需求编号 → 迭代 → 资产 → 测试 ID”的可维护追踪模板与填表规则。
- `progress/I02.md`：本迭代变更总结。

## 修改

- `packages/contracts/src/index.ts`：定义版本化共享 DTO、分页、金额、响应包络、错误码、幂等协议、稳定请求序列化与四类已结算经济事实事件。
- `packages/contracts/package.json`：契约包测试在存在测试文件时必须实际执行。
- `apps/api/docs/api/README.md`：记录 I02 响应包络、分页、金额、错误码、幂等键与事实事件的对外协议。
- `完整项目迭代实施计划与检查清单.md`：勾选已实现并验证的 I02 checklist。

## 删除

无。

## 特殊点

- 权威边界：事实事件仅陈述已完成结果；没有任何事件携带执行扣款、库存变更、开奖或赛事结算的命令。
- 契约与兼容性：`CONTRACTS_VERSION` 为 `2026-07-24`，四类事实事件当前均固定为 `version: 1`；未来破坏性变更必须新增版本或事件类型。
- 幂等与审计：同一调用者和幂等键必须关联规范化请求的 SHA-256 指纹；同键异参使用 `IDEMPOTENCY_CONFLICT`，持久化唯一约束负责并发下至多一次业务结果。
- 已验证：`pnpm --filter @mtg-market/contracts test`（4 项通过）、`pnpm check` 与 `pnpm format:check` 均于 2026-07-24 通过。
