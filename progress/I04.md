# I04 完成记录

日期：2026-07-24  
迭代：I04：API 横切能力与可观测性

## 新增

- `apps/api/src/app.ts`：可注入 Fastify 应用工厂；提供请求 ID、统一错误包络、Zod 输入校验、CORS 白名单、Pino 脱敏、写请求审计、健康与就绪路由。
- `apps/api/src/openapi.ts`：OpenAPI 3.1 最小文档源与公开路由清单。
- `apps/api/src/shared/http/api-response.ts`：contracts 响应包络构造函数。
- `apps/api/src/shared/http/request-context.ts`：受校验的请求 ID 提取与生成。
- `apps/api/src/app.test.ts`：Fastify inject 覆盖健康、就绪、校验失败、未知路由、CORS 与 OpenAPI 路由校验。
- `progress/I04.md`：本迭代完成记录。

## 修改

- `apps/api/src/server.ts`：改为仅组合应用工厂、数据库、任务循环与进程生命周期。
- `apps/api/src/config/environment.ts`：增加并校验逗号分隔的 `CORS_ORIGINS` 白名单，未配置时回退到 `WEB_ORIGIN`。
- `apps/api/src/config/environment.test.ts`：覆盖 CORS 白名单默认和规范化行为。
- `apps/api/.env.example`：说明并提供 `CORS_ORIGINS` 示例。
- `apps/api/ARCHITECTURE.md`：登记 I04 HTTP 横切职责和边界。
- `apps/api/docs/api/README.md`：登记统一包络、健康端点、CORS、OpenAPI 与日志/审计协议。
- `apps/api/docs/operations/README.md`：登记存活/就绪探针和故障处置约定。
- `后端需求.md`：登记 HTTP 横切运行要求。
- `完整项目迭代实施计划与检查清单.md`：勾选 I04 已验证 checklist。
- `项目协作文档索引.md`：将协作基线更新为 I04 完成。

## 删除

无。

## 特殊点

- 请求 ID、错误码和响应包络以 `@mtg-market/contracts` 为唯一契约来源；金额和业务规则不在 HTTP 层结算。
- API 使用 Fastify 内置 Pino，并脱敏认证、Cookie、API key、密码和 token 字段；审计不保存原始请求体。I06 认证完成前写审计的 `actor_id` 为 `NULL`，届时必须接入可信身份上下文。
- `/ready` 仅以 SQLite 查询和 `jobs` 状态聚合作为当前阶段健康摘要；I05 任务租约/执行器上线后应扩展任务活跃度与失败告警判断。
- 验证证据：`pnpm --filter @mtg-market/api test`（8 passed）与 `pnpm check` 于 2026-07-24 通过。
