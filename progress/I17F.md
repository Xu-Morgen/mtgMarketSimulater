# I17F 完成记录

日期：2026-07-27

## 新增

- `apps/web/app/(player)/market/history/page.tsx`：App Router 入口，仅组合 `PriceHistoryPage` 并以 Suspense 包裹 `useSearchParams`，避免整条路由退化为客户端渲染。
- `apps/web/features/market/price-history-page.tsx`：价格历史与市场曲线页面编排。只读服务端 `GET /v1/market/index/history` 与 `GET /v1/market/quotes/{skuId}/history` 渲染双曲线；通过 `GET /v1/market/quotes?limit=20` 候选列表选择单卡；展示 `PublicPriceStatusDto` 来源/更新时间/过期状态/固定 disclaimer；空 points、查询失败与 `stale` 旧价降级均有明确反馈。
- `apps/web/features/market/price-history-page.module.css`：页面布局、范围切换按钮组、SKU 候选列表、降级表格容器与窄屏样式。
- `apps/web/components/market/price-history-chart.tsx`：ECharts canvas 双折线组件（`role="img"` + 中文 `aria-label`）。`connectNulls=false` 让缺失参考价/游戏内价的自然日断线，不插值；接收服务端已按日采样的序列。
- `apps/web/components/market/price-history-chart.module.css`：图表容器、图例与色块样式。
- `apps/web/tests/e2e/price-history.spec.ts`：Playwright（桌面 + 390 × 844 窄屏）。覆盖默认 30d、范围切换写 URL 并重查、单卡双曲线/降级表格/空历史、`freshness=stale` 旧价降级、查询失败重试与窄屏不阻断；请求断言验证 `range` 由服务端解析。
- `apps/web/tests/manual/I17F.md`：FE-MARKET/FE-CATALOG 人工验收记录。

## 修改

- `apps/web/package.json`：新增 `echarts: ^5.6.0` 依赖（技术栈 §2 推荐的图表库；不引入 framer-motion，本页无动画需求）。
- `apps/web/api/market-api.ts`：新增 `marketApi.history`/`marketApi.indexHistory` 与 `usePriceHistoryQuery(skuId, range)`/`useMarketIndexHistoryQuery(range)`；查询键按用户、skuId、range 隔离，`retry: false`。
- `apps/web/features/market/market-page.tsx`：导言段加入“价格历史与市场曲线”链接，指向 `/market/history`。
- `apps/api/src/modules/jobs/task-worker.test.ts`：`prefer-const` 机械修正（`jobs` 仅被读取一次），不放宽断言；修复该 lint 失败以恢复 `pnpm check` 门禁。
- `apps/web/ARCHITECTURE.md`、`前端需求.md`、`完整项目迭代实施计划与检查清单.md`、`模拟器主流程与核心验收.md`、`项目协作文档索引.md`：I17F 从“计划”更新为实现细节（路由、ECharts 双曲线、范围/URL 语义、降级与空态、来源说明），勾选 I17F checklist、状态行改为“下一可执行轨道为 I18B”、追加 AT-03B 浏览器证据与协作基线段。

## 删除

- 无。

## 特殊点

- 权威边界：页面只读 `price_snapshot_entries`/`market_quotes` 的按日采样投影，不在浏览器插值、重算指数或汇率；图表 null 点 `connectNulls=false` 断线，不掩盖空态。`range`/`skuId` 写入 URL 仅在用户切换或选择时发生；默认 30d 不强制写入 URL（与市场页“筛选只在变更时写 URL”的既有约定一致）。
- 依赖与契约：新增 `echarts@^5.6.0`（npm registry；aliyun 私有镜像无授权且缺 `zrender@6.1.0`，故改用 npmjs 稳定线），未引入 framer-motion。复用 I17B 已有的 `PriceHistoryDto`/`MarketIndexHistoryDto`/`PublicPriceStatusDto.disclaimer`，未新增 DTO 或修改后端。
- 事务与审计：本轮无后端写操作或迁移；`pnpm check` 与 74 个 API 测试、10 个 Playwright 用例（chromium + 窄屏）均通过。
- 已知限制：仓库的 Playwright 全量并行运行存在既有交叉污染（多个 spec 共享 `/tmp/mtg-i06f-playwright.db` 与 global-setup seed），高并发时全量套件会出现与本次变更无关的偶发失败；隔离运行 `price-history.spec.ts`、`market.spec.ts`、`inventory.spec.ts`、`npc-buy/sell.spec.ts`、`auth.spec.ts` 等均稳定通过。该并行污染不属于 I17F 范围，未在本轮改 `playwright.config.ts` 的 webServer/db 隔离策略。
- 验证：`pnpm check`（lint + 全 workspace `tsc --noEmit`）、`pnpm --filter @mtg-market/api test`（74）、`pnpm --filter @mtg-market/web test:e2e tests/e2e/price-history.spec.ts`（10）。
