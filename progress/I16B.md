# I16B 完成记录

日期：2026-07-27

## 新增

- 无。

## 修改

- `packages/contracts/src/index.ts`：新增 `NpcSellPreviewDto`，并更新共享契约版本。
- `apps/api/src/modules/orders/application/npc-trade-service.ts`：实现 NPC 卖出预览和确认、最低价保护、可用库存校验、幂等、事实事件、outbox、重定价与审计编排。
- `apps/api/src/modules/orders/api/npc-trade-routes.ts`：新增卖出预览与确认 HTTP 端点，支持 `quantity=all` 的服务端解析。
- `apps/api/src/modules/orders/api/npc-trade-routes.test.ts`：覆盖成功、全部可用库存、并发重放、同键异参、锁定库存、最低价、额度与事务回滚。
- `apps/api/src/modules/market/application/market-service.ts`：通过应用接口按交易方向读取 NPC 出售价或收购价，避免 Orders 直接读取市场表。
- `apps/api/src/modules/inventory/application/inventory-service.ts`：提供同一经济短事务内只扣减可用库存的受控出售入口。
- `apps/api/src/modules/users/application/user-service.ts`、`apps/api/src/modules/users/infrastructure/sqlite-user-repository.ts`：提供并实现 `npc_sell` 的受控余额收入与 credit 账本入口。
- `apps/api/src/openapi.ts`：登记卖出预览与确认端点。
- `apps/api/ARCHITECTURE.md`、`apps/api/docs/api/README.md`、`apps/api/docs/operations/README.md`、`后端需求.md`：记录 I16B 的模块、HTTP、运维和不变量边界。
- `完整项目迭代实施计划与检查清单.md`、`模拟器主流程与核心验收.md`、`项目协作文档索引.md`：更新 I16B 完成状态、AT-04 服务端证据和下一轨道。

## 删除

- 无。

## 特殊点

- 权威边界：卖出只通过 Market、Users、Inventory 的 application 接口协作；Orders 不直接写对方表。
- 契约：`quantity=all` 仅存在于预览请求并解析为当前 `availableQuantity`；确认使用该解析后的正整数，批量出售未实现。
- 幂等与审计：成功路径在一笔 SQLite 短事务内写 `npc_sell` 账本/库存流水、`npc_trades`、事实事件、outbox、重定价任务、审计和幂等响应；任何写入异常会整体回滚。
- 验证：已通过 `pnpm --filter @mtg-market/api test -- npc-trade-routes.test.ts` 与 `pnpm --filter @mtg-market/api check`；全 workspace `pnpm check` 留待本次变更结束前执行。
