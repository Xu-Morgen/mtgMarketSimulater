# Jobs Application

定义入队、调度、恢复与任务处理用例。`catalog.sync`、`prices.sync`、`daily.rollover`、`market.reprice`、`tournament.settle`、`order.expire` 与 `backup.create` 均应从这里被编排。`narrative.generate` 仅为 I33 发布后 I34 的可选预留任务，首发不得投递或领取。
