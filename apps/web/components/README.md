# 组件层（components）

本层存放可复用的视觉与交互单元：基础 UI、表单字段、业务展示组件、价格图表、开包与比赛动画，以及加载、空状态和错误状态组件。组件应接收明确的 props，保持可复用和可测试。

组件可使用 `constants/` 和 `utils/`；需要 UI 临时状态时可消费 `stores/`。业务展示组件优先由页面接收服务端数据后传入，避免自行推导经济或比赛结果。不得在此层计算可结算金额、价格、奖励或概率；不得直接绕过 `api/` 发起请求。

现有组件会在后续开发中按基础组件、领域组件和可视化组件逐步归类；本次不迁移既有实现。
