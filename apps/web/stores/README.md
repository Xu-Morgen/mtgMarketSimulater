# 状态管理层（stores）

本层仅使用 Zustand 管理可丢弃、未提交的客户端 UI 状态，例如市场筛选条件、排序、界面偏好、开包动画阶段、弹窗状态和未保存的卡组草稿。

余额、库存、订单、价格、开包结果、比赛结果、奖励、成就与权限等服务器真相必须由 TanStack Query 管理，不得写入本层。本层不得请求 API，也不得根据本地状态推导可结算值。

每个 store 应按业务场景命名，并明确初始状态、可重置边界和是否允许持久化；只有不影响经济结算的用户偏好可以考虑持久化。
