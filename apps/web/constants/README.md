# 常量层（constants）

本层存放不随运行时服务器数据变化的前端声明，例如路由名称、展示枚举映射、界面默认值、查询键命名和静态限制提示。常量应按领域拆分，避免形成无归属的大型全局文件。

本层不得放置 API 响应缓存、用户数据、动态价格、环境密钥、可配置经济参数或任何需要服务端审计的规则。需要随服务端调整的市场参数、概率、费用、赛事配置与权限必须经由 `api/` 查询获得。

常量层不依赖页面、组件、store 或 API 层，以保持最底层依赖稳定。
