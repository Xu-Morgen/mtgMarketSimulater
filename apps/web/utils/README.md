# 工具层（utils）

本层包含无副作用、可独立测试的通用函数，例如金额、数量、日期和价格来源的展示格式化，查询参数解析，表单输入的非结算校验，以及 idempotency key 生成。

工具函数不应发起网络请求、访问 React 生命周期、读写 Zustand 或 TanStack Query 缓存，也不应包含页面布局和业务组件。它们只能处理已给定的数据，不能计算服务端权威的费用、保证金、报价、奖励、开包或赛果。

如规则具有服务端结算含义，应放在共享规则包或由服务端返回，不得加入本目录。
