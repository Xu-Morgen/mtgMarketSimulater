# I23F：每日工作资金领取页面人工验收

日期：2026-07-29

- 执行人：Codex。
- 构建/提交标识：工作区 I23F 变更；`pnpm --filter @mtg-market/web check` 通过。
- 浏览器：Chromium 桌面与 Chromium 390 × 844 窄屏。
- 测试环境：本地 Next.js/Fastify 隔离测试服务；玩家页只读本地 `GET /v1/daily-work-funding`，领取只以幂等键调用 `POST /v1/daily-work-funding/claim`，未请求 Scryfall、MTGJSON 或 OpenAI。
- 测试账号与数据：隔离玩家 `10000000-0000-4000-8000-00000000023f`、存档 `archive-i23f`、领取记录 `daily-claim-i23f`、规则版本 `daily-work-funds/v1`；未记录密码、令牌或 Cookie。
- 截图/录屏：无；Playwright 在失败时保留 trace/screenshot，运行无失败附件。

## FE-DASH / AT-02：领取、重复点击、刷新与跨日

- 前置数据：服务器日切已开放 `2026-07-29`（`Asia/Shanghai`）的 1,000 游戏币资格，账本夹具包含 `daily_work_funding` credit 流水。
- 操作步骤：打开 `/dashboard`，核对服务端日期、时区、金额、规则版本、服务器开放时间和下一次可领取时间；点击「领取 1,000 游戏币」并快速双击；刷新后复核「今日已领取」、领取记录、禁用按钮和账本类型。以已领取状态重新打开页面，确认不投递领取请求。再模拟服务器在请求期间切换为 `2026-07-30`（`America/New_York`）并返回 `RESOURCE_CONFLICT`，核对失败信息后的资格、日期、时区和下一次时间。
- 预期结果：资格、金额、日期、规则、领取记录、下一次时间和账本均取服务器 DTO；双击只产生一次空请求体 + UUID 幂等键的领取请求。成功、冲突或跨日后重新查询服务器，页面不以浏览器本地日期决定资格；窄屏所有信息和操作均可见。
- 实际结果：通过。可领取状态显示 `2026-07-29（Asia/Shanghai）` 与 1,000 游戏币；快速双击只发送 1 个 POST，成功后刷新为「今日已领取」，领取按钮禁用并显示领取记录，账本显示「每日工作资金」。已领取初始状态未发送 POST。跨日冲突显示服务端错误后，页面刷新为 `2026-07-30（America/New_York）`，下一次时间按该服务端时区显示。桌面与 390 × 844 窄屏均未出现横向阻断。
- 证据：`apps/web/tests/e2e/daily-work-funding.spec.ts`；命令 `E2E_DATABASE_PATH=/tmp/mtg-i23f-playwright-3.db pnpm --filter @mtg-market/web test:e2e --project=chromium --project=narrow-chromium tests/e2e/daily-work-funding.spec.ts`，6 passed。
- 结果：通过。
