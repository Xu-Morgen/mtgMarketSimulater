# I10F 人工验收记录

- 日期：2026-07-24
- 执行人：Codex
- 构建：本地 Next.js/API 开发构建；`TMPDIR=/tmp TEMP=/tmp TMP=/tmp pnpm --filter @mtg-market/web exec playwright test tests/e2e/inventory.spec.ts --project=chromium --workers=1` 与同命令的 `--project=narrow-chromium`。
- 浏览器：Chromium（桌面）与 Chromium 390 × 844（窄屏）。
- 测试数据：隔离 SQLite `/tmp/mtg-i06f-playwright.db` 的新注册玩家；Playwright 在浏览器层模拟本地 `/v1/inventory` 的持仓、无价、空和错误响应。
- 截图/录屏：无；专项用例成功，失败时才保留 Playwright trace。页面请求均为本地 `http://localhost:3001/v1/inventory`。

## I10F：库存总览

- 前置数据：包含 4 张持有、1 张可用、2 张订单锁定、1 张比赛锁定的 SKU；该 SKU 没有有效市值快照。
- 操作步骤：打开“我的库存”，依次检查数量与无价说明；应用工艺和锁定筛选，切换数量排序，进入第二页；再分别模拟空库存、500 错误并点击刷新/重试恢复。
- 预期结果：页面只显示服务端返回的数量、平均成本和市值/原因；筛选、排序和分页写入 URL；空态、错误与恢复操作清晰，窄屏可完成流程。
- 实际结果：通过。页面显示持有/可用/订单锁定/比赛锁定与“尚无有效价格快照，暂不显示市值。”；URL 可恢复 `finish`、`locked`、`sort`、`cursor`；空态、加载失败和重试恢复均正确，窄屏导航和表格横向滚动未阻断操作。
- 证据：I10F 专项 Playwright 共 2 个场景，在桌面和窄屏项目均通过；请求地址断言均为本地库存 API。
- 结果：通过

结论：I10F 页面没有直接改库存、成本、市值或解锁资产的入口；所有展示值均来自库存查询响应。
