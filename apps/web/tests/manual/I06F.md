# I06F 前端基础、认证页面与人工验收基线

- 执行日期：2026-07-24
- 执行结果：通过（用户确认）
- 构建标识：当前工作区 I06F 变更集
- 浏览器：Chromium（桌面与窄屏）
- 测试数据：隔离 SQLite 管理员 `admin@local.test`、临时玩家账号；未记录密码、Cookie 或令牌。
- 自动化佐证：`pnpm --filter @mtg-market/web run test:e2e --project=chromium`，3 passed；`pnpm check` 通过。

## FE-M01 / FE-M07：认证、会话与管理路由

- 操作：完成玩家注册/登录、刷新恢复、退出、错误密码；完成管理员登录；以玩家直接访问 `/admin` 并请求管理 API；在桌面与窄屏复核认证页面、焦点与导航。
- 预期结果：玩家进入 `/dashboard`，管理员进入 `/admin`；刷新恢复会话；退出后返回登录页；错误密码显示服务端错误；玩家管理页面显示 403，管理 API 返回 403；登录后侧栏不显示模拟器说明。
- 实际结果：与预期一致；密码显示/隐藏、返回模拟器说明入口、加载/无权限状态均可操作。
- 证据：Playwright Chromium 三项认证用例通过；本次人工验收由用户确认通过。
- 结果：通过
