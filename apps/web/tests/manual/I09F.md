# I09F 人工验收记录

- 日期：2026-07-24
- 构建：本地 Next.js/API 开发构建；`TMPDIR=/tmp TEMP=/tmp TMP=/tmp pnpm --filter @mtg-market/web exec playwright test tests/e2e/admin-catalog-sync.spec.ts --workers=1`
- 浏览器：Chromium（桌面）与 Chromium 390 × 844（窄屏）
- 测试数据：隔离 SQLite `/tmp/mtg-i06f-playwright.db` 的管理员账号；Playwright 在浏览器层以本地管理 API 响应夹具模拟最近成功版本、失败运行与等待任务，后端 API 注入测试验证真实 403、幂等投递与状态 DTO。
- 截图/录屏：无；Playwright 仅在失败时保留截图和 trace。本次 I09F 专项 6 个浏览器用例全部通过，目录同步 API 路由断言均为本地 `http://localhost:3001/v1/admin/*`。

| 步骤 | 结果 |
| --- | --- |
| 管理员登录并从管理导航进入“目录同步” | 通过；展示最近成功版本、导入差异和当前任务；窄屏信息卡单列显示。 |
| 点击“触发目录同步”并在二次确认框中确认 | 通过；仅提交本地管理 API，生成可追踪任务 ID；任务等待时触发按钮禁用，避免重复提交。 |
| 查看失败摘要与最近成功版本，确认重试失败任务 | 通过；页面明确说明旧目录保留，显示失败原因；二次确认后任务重新排队。 |
| 普通玩家查看导航、直接访问 `/admin/catalog-sync` 并请求管理 API | 通过；没有管理入口，页面显示无权限，`GET /v1/admin/catalog/sync` 返回 403。 |

结论：I09F 通过。浏览器不直连 Scryfall；同步的经济与目录写入仍由服务端任务处理，页面只展示脱敏服务端状态并提交经确认的幂等意图。
