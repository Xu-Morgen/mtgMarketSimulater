# I08F 人工验收记录

- 日期：2026-07-24
- 构建：本地 Next.js/API 开发构建（`pnpm --filter @mtg-market/web test:e2e -- catalog.spec.ts`）
- 浏览器：Chromium（桌面）与 Chromium 390 × 844（窄屏）
- 测试数据：隔离 SQLite `/tmp/mtg-i06f-playwright.db` 中的 `ONE` 系列目录夹具；同名的三个 SKU 分别为非闪、闪和蚀刻，第三个 SKU 无图片缓存。
- 截图/录屏：无；Playwright 配置仅在失败时保留截图和 trace。本次 14 个浏览器用例均通过（含既有认证回归）。

| 步骤 | 结果 |
| --- | --- |
| 登录玩家，打开“卡牌目录”，查看同名卡牌的首页及下一页 | 通过；目录按服务端游标分页展示，同名卡牌的印刷 SKU 与工艺没有合并。 |
| 打开无缓存图片的 SKU 详情 | 通过；页面显示“暂无图片”及文字降级说明，没有浏览器外部请求。 |
| 按工艺筛选后刷新页面 | 通过；`finish=foil` 保留在 URL，刷新后选择器和服务端结果均恢复。 |
| 输入不存在的名称筛选 | 通过；显示可理解的无结果状态。 |
| 模拟本地目录 API 返回失败 | 通过；显示“卡牌目录加载失败”和可操作的重试入口。 |
| 在 390px 窄屏重复目录、分页和详情流程 | 通过；筛选、分页、详情返回入口均可操作，无横向阻断。 |

结论：I08F 通过。目录只读取本地 Fastify API；印刷 SKU、来源和图片缓存状态均来自服务端 DTO，浏览器未直连外部 Provider。
