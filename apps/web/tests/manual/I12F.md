# I12F 人工验收记录

- 日期：2026-07-26
- 执行人：Codex
- 构建：本地 Next.js/API 开发构建；`TMPDIR=/tmp TEMP=/tmp TMP=/tmp pnpm --filter @mtg-market/web exec playwright test tests/e2e/packs.spec.ts --workers=1`。
- 浏览器：Chromium（桌面）与 Chromium 390 × 844（窄屏）。
- 测试数据：隔离 SQLite `/tmp/mtg-i06f-playwright.db` 的新注册玩家；浏览器层仅模拟本地 Fastify 的补充包、购买预览、开包和开包历史响应。
- 截图/录屏：无；专项用例成功，失败时才保留 Playwright trace。未访问 Scryfall、MTGJSON 或 OpenAI。

## FE-M03 / AT-03A：查看概率、购买、连续点击、刷新结果与历史

- 前置数据：一个已启用的 `pack/v1` 测试补充包，价格 500 游戏币，包含两项服务端已结算的 SKU 结果；另有一个停用原因是“活动已结束”的补充包。
- 操作步骤：打开补充包商店，核对概率、规则版本和禁用原因；选择已启用补充包，核对服务端购买预览的成本和规则版本，确认购买；在提交期间连续点击确认；结果出现后跳过动画，进入并刷新开包历史。
- 预期结果：确认期间同一按钮被禁用并只发起一次开包请求；动画只揭晓服务端响应，可跳过；结果和历史只显示该一次结算，刷新历史不重新扣款或开包。
- 实际结果：通过。浏览器只读取本地预览并回传服务端 `pack/v1`，确认按钮提交中为禁用状态；跳过后显示两项服务端 SKU 结果和 I17 前价格不可用提示。历史刷新后仍只显示已结算记录。
- 证据：`packs.spec.ts` 成功场景检查开包请求数为 1、幂等键存在、动画跳过和两次历史读取；桌面及窄屏均执行。
- 结果：通过

## FE-M03 / AT-03A：余额不足、版本过期与失败恢复

- 前置数据：同一补充包先返回 `insufficient_balance` 预览，再返回可购买预览；开包命令返回 `VERSION_STALE`。
- 操作步骤：打开预览，确认余额不足时的提示与禁用按钮；关闭后重新打开预览并确认，观察版本过期错误。
- 预期结果：余额不足不能提交；版本过期提供服务端错误，不出现虚构卡牌或结果页；用户可关闭并重新预览。
- 实际结果：通过。余额不足提示为“可用余额不足”，确认按钮不可用；版本过期原样显示服务端“补充包规则版本已更新，请重新确认购买”，没有开包结果。
- 证据：`packs.spec.ts` 的失败场景断言开包请求数、服务端错误文案和结果页不存在；桌面及窄屏均执行。
- 结果：通过

结论：I12F 浏览器不持有候选池、种子、概率规则或经济真相；动画 Zustand 状态可丢弃，所有可见结果均来自 Fastify 已结算 DTO。
