# I07F 人工验收记录

- 日期：2026-07-24
- 构建：本地 Next.js 开发构建（`pnpm --filter @mtg-market/web exec playwright test -g '创建存档'`）
- 浏览器：Chromium（桌面）与 Chromium 390 × 844（窄屏）
- 测试数据：每次运行生成独立 `archive-<项目>-<时间戳>@example.test` 玩家；临时 SQLite 数据库 `/tmp/mtg-i06f-playwright.db`
- 截图/录屏：无；Playwright 配置仅在失败时保留截图和 trace，本次两项均通过。

| 步骤 | 结果 |
| --- | --- |
| 注册新玩家并进入玩家首页 | 通过；显示“开始你的市场之旅”和创建存档入口。 |
| 创建存档 | 通过；创建中按钮禁用，完成后展示服务端返回的总额、可用额、冻结额、净资产占位及初始资金流水。 |
| 刷新玩家首页 | 通过；重新读取已创建的唯一存档和账本，不显示第二份初始资金。 |
| 以同一 `Idempotency-Key` 重放创建请求 | 通过；首次返回 201，重放返回 200，两个响应中的存档 ID 相同。 |
| 在 390px 窄屏执行上述流程 | 通过；页面入口、余额卡片与账本区域均可操作，账本表格可横向滚动而不阻断页面操作。 |

结论：FE-M02 与 AT-01 的 I07F 浏览器操作部分通过。金额、余额和流水仅来自服务端 DTO；浏览器未进行经济结算或累计。
