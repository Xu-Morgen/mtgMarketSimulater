# I11F 人工验收记录

- 日期：2026-07-26
- 执行人：Codex
- 构建：本地 Next.js/API 开发构建；`TMPDIR=/tmp TEMP=/tmp TMP=/tmp pnpm --filter @mtg-market/web exec playwright test tests/e2e/packs.spec.ts --workers=1`。
- 浏览器：Chromium（桌面）与 Chromium 390 × 844（窄屏）。
- 测试数据：隔离 SQLite `/tmp/mtg-i06f-playwright.db` 的新注册玩家；Playwright 在浏览器层模拟本地 `/v1/packs` 和 `/v1/packs/{packId}` 的版本化补充包、禁用、空与失败响应。
- 截图/录屏：无；专项用例成功，失败时才保留 Playwright trace。浏览器层路由桩仅模拟本地 `http://localhost:3001/v1/packs` 及其详情路径。

## I11F：补充包列表与概率公示

- 前置数据：一个已启用的 `pack/v1` 测试补充包，卡位 `regular` 每包抽取 2 次，普通 9,000 bp、稀有 1,000 bp；另有一个停用原因是“活动已结束”的补充包。
- 操作步骤：打开“补充包商店”，核对价格、规则版本和禁用原因；进入概率详情，核对卡位、稀有度概率和无保底文案；再分别模拟加载、空列表、500 错误并点击重试返回 `pack/v2`。
- 预期结果：列表与详情只展示 API 的价格、状态、规则版本和概率；禁用包说明不可购买原因；页面不出现购买/开包入口，也不显示或保存保底进度；桌面和窄屏均可阅读卡位概率表。
- 实际结果：通过。概率显示为服务端返回的 basis points 及其展示百分比，禁用原因、`pack/v1`/`pack/v2` 变化、空态与失败重试均正确；页面明确 MVP 没有保底，且没有购买或开包按钮。
- 证据：I11F 专项 Playwright 共 2 个场景，在桌面和窄屏项目均通过（共 4 个测试）；请求路由断言均只命中本地补充包 API。
- 结果：通过

结论：I11F 前端不含抽卡规则、候选池、随机种子、开奖结果或保底状态；服务端仍是概率与未来开包结果的唯一权威。
