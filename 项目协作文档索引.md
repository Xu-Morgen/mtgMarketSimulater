# 项目协作文档索引

> 目的：为多人或多 Agent 协作建立单一事实来源与同步规则，避免相同决策、状态或验收结果在不同文档中重复维护后发生漂移。

## 使用规则

1. 开始实现前，先阅读本索引和与改动范围匹配的权威文档。
2. 在同一变更内更新被影响的权威文档；其他文档只添加链接或简短引用，不复制完整规则。
3. 记录已验证事实时附稳定标识和日期，例如 `I12`、`AT-03`、`ADR-001`、迁移号、规则版本或任务类型。
4. 尚未验证的内容标记为“待决”或“计划”，不得勾选实施计划或写成已完成。
5. 本索引只维护文档职责与同步机制，不复制各文档的业务内容。

## 核心文档登记

| 文档 | 唯一事实范围 | 必须实时更新的触发条件 | 关联文档 |
| --- | --- | --- | --- |
| [完整项目迭代实施计划与检查清单.md](完整项目迭代实施计划与检查清单.md) | 迭代顺序、每期 checklist、验收门槛、MVP 发布门禁 | 开始/完成某项 checklist、调整依赖或验收条件、发现阻断风险 | 所有需求、架构与验收材料 |
| [技术栈与模块职责边界.md](技术栈与模块职责边界.md) | 全局技术选型、服务拓扑、权威边界、跨端职责、部署边界 | 新增/替换基础设施、改变数据所有权、模块职责或外部 Provider 边界 | 各应用 ARCHITECTURE、ADR |
| [模拟器主流程与核心验收.md](模拟器主流程与核心验收.md) | 玩家主流程、业务不可变量、AT-01 至 AT-12 的验收定义 | 主流程、业务规则或验收步骤变更；验收执行结果/证据产生 | 实施计划、产品计划书、测试资产 |
| [产品计划书-卡牌市场模拟器.md](产品计划书-卡牌市场模拟器.md) | 产品范围、需求分级、体验目标、候选迭代 | 新需求、范围裁剪、优先级、产品规则或页面导航变化 | 前/后端需求、实施计划 |
| [前端需求.md](前端需求.md) | 前端功能、交互、页面级验收与工程要求 | 页面/交互/API 消费方式、加载空错态或前端验收变化 | `apps/web/ARCHITECTURE.md`、实施计划 |
| [后端需求.md](后端需求.md) | 后端模块、业务接口、任务、不变量与运行要求 | 模块能力、事务边界、端点、任务或不变量变化 | `apps/api/ARCHITECTURE.md`、contracts、ADR |
| [AI需求.md](AI需求.md) | AI 输入输出、权限、降级和验收要求 | 模型调用策略、Schema、预算、权限或降级策略变化 | `apps/ai/ARCHITECTURE.md`、实施计划 I28–I29 |
| `apps/web/ARCHITECTURE.md` | Web 目录分层、依赖方向、状态归属和迁移约定 | 新增/调整 Web 层、目录职责、Provider、缓存或状态边界 | 前端需求、技术边界 |
| `apps/api/ARCHITECTURE.md` | API 分层、模块间协作、源码目录与数据目录边界 | 新增/调整 API 模块、端口、基础设施或事务编排方式 | 后端需求、技术边界、ADR |
| `apps/ai/ARCHITECTURE.md` | AI 分层、任务流、数据访问和 Provider 隔离 | 新增/调整 AI use case、端口、任务、策略或 Provider 实现 | AI需求、技术边界 |
| `packages/contracts/src/index.ts` | 运行时/编译时共享 DTO、错误、包络、事件与幂等契约 | 任一跨层请求、响应、错误码、事件或序列化规则变化 | API 文档、前端 API 客户端、契约测试 |
| `packages/rules/src/` | 可结算纯规则、输入输出、版本与可重放行为 | 开包、报价、订单、卡组、赛事、成就等规则或参数变化 | 规则测试、后端用例、实施计划 |
| `apps/api/docs/adr/` | 已作出的重要且持久的技术/业务实现决策与取舍 | 选择或拒绝会影响多个模块、数据迁移、运维或安全边界的方案 | 技术边界、各 ARCHITECTURE |
| `apps/api/docs/api/` | 对外 HTTP 协议、错误语义、版本兼容说明 | 端点、认证、响应包络、错误码、幂等行为或 OpenAPI 变化 | contracts、后端需求 |
| `apps/api/docs/operations/` | 运行、同步、备份、恢复、告警与人工处置步骤 | 新任务、数据源、备份/恢复、部署、监控或故障处置变化 | 实施计划 I05/I17/I22/I31/I32 |
| `README.md` | 项目概览、目录入口、最低启动路径和安全提醒 | workspace 入口、启动命令、开发前置条件或公开架构概览变化 | 技术边界、各应用 README |

## 代码与文档的同步矩阵

| 发生的变更 | 同步动作 |
| --- | --- |
| 新增/变更 API、DTO、错误码或幂等行为 | 更新 `packages/contracts`、`apps/api/docs/api/`、相关前端 API 调用和测试；若影响需求，更新前/后端需求。 |
| 新增/变更可结算规则 | 更新 `packages/rules` 与规则测试；记录规则版本；同步后端需求、验收定义和受影响迭代 checklist。 |
| 新增表、迁移、事务、不变量或异步任务 | 更新 API 架构、后端需求、运维文档、集成测试和实施计划；重大取舍新增 ADR。 |
| 新增或改变页面、状态归属、交互流程 | 更新 Web 架构、前端需求、主流程验收和端到端测试。 |
| 新增/变更 AI Provider、Schema、预算、降级或权限 | 更新 AI 架构、AI需求、运维文档、测试和 I28–I29 checklist。 |
| 变更部署、环境变量、数据卷、备份或监控 | 更新 `.env.example`、README、运维文档、技术边界和 I31/I32 checklist。 |
| 完成迭代项或验收项 | 先保存测试/演示证据与必要的运维说明，再勾选实施计划中的对应项；同步主流程验收的执行状态。 |

## 当前协作基线（2026-07-23）

- 当前实施计划状态：I01–I32 的业务 checklist 均未勾选；仓库仅具备启动骨架、健康检查、SQLite WAL 初始化、`jobs` 表/轮询骨架、AI Schema/模板骨架与共享类型/NPC 报价示例。
- 当前首要工作：按 I01 收口工程约束与本地开发基线，再进入 I02 的共享契约与幂等协议。
- 尚未建立的正式记录：ADR、版本化 API 文档、运维手册内容、测试证据与规则版本登记。创建这些内容后，应在本表对应位置登记其稳定链接或编号规则。

