# 项目协作文档索引

> 目的：为多人或多 Agent 协作建立单一事实来源与同步规则，避免相同决策、状态或验收结果在不同文档中重复维护后发生漂移。

## 使用规则

1. 开始实现前，先阅读本索引和与改动范围匹配的权威文档。
2. 在同一变更内更新被影响的权威文档；其他文档只添加链接或简短引用，不复制完整规则。
3. 记录已验证事实时附稳定标识和日期，例如 `I12B`、`I12F`、`AT-03`、`ADR-001`、迁移号、规则版本或任务类型。
4. 尚未验证的内容标记为“待决”或“计划”，不得勾选实施计划或写成已完成。
5. 本索引只维护文档职责与同步机制，不复制各文档的业务内容。

## 核心文档登记

| 文档 | 唯一事实范围 | 必须实时更新的触发条件 | 关联文档 |
| --- | --- | --- | --- |
| [完整项目迭代实施计划与检查清单.md](完整项目迭代实施计划与检查清单.md) | 迭代顺序、每期 checklist、验收门槛、MVP 发布门禁 | 开始/完成某项 checklist、调整依赖或验收条件、发现阻断风险 | 所有需求、架构与验收材料 |
| [技术栈与模块职责边界.md](技术栈与模块职责边界.md) | 全局技术选型、服务拓扑、权威边界、跨端职责、部署边界 | 新增/替换基础设施、改变数据所有权、模块职责或外部 Provider 边界 | 各应用 ARCHITECTURE、ADR |
| [模拟器主流程与核心验收.md](模拟器主流程与核心验收.md) | 玩家/管理员主流程、业务不可变量、AT-01 至 AT-13 的验收定义 | 主流程、业务规则或验收步骤变更；验收执行结果/证据产生 | 实施计划、产品计划书、测试资产 |
| [产品计划书-卡牌市场模拟器.md](产品计划书-卡牌市场模拟器.md) | 产品范围、需求分级、体验目标、候选迭代 | 新需求、范围裁剪、优先级、产品规则或页面导航变化 | 前/后端需求、实施计划 |
| [前端需求.md](前端需求.md) | 玩家/管理前端功能、交互、页面级验收 ID、人工验收与工程要求 | 页面/交互/API 消费方式、加载空错态、管理操作或前端验收变化 | `apps/web/ARCHITECTURE.md`、实施计划 |
| [后端需求.md](后端需求.md) | 后端模块、业务接口、任务、不变量与运行要求 | 模块能力、事务边界、端点、任务或不变量变化 | `apps/api/ARCHITECTURE.md`、contracts、ADR |
| [AI需求.md](AI需求.md) | AI 输入输出、权限、降级和验收要求 | 模型调用策略、Schema、预算、权限或降级策略变化 | `apps/ai/ARCHITECTURE.md`、实施计划 I28AI/I29AI/I32AI |
| `apps/web/ARCHITECTURE.md` | Web 目录分层、依赖方向、状态归属和迁移约定 | 新增/调整 Web 层、目录职责、Provider、缓存或状态边界 | 前端需求、技术边界 |
| `apps/api/ARCHITECTURE.md` | API 分层、模块间协作、源码目录与数据目录边界 | 新增/调整 API 模块、端口、基础设施或事务编排方式 | 后端需求、技术边界、ADR |
| `apps/ai/ARCHITECTURE.md` | AI 分层、任务流、数据访问和 Provider 隔离 | 新增/调整 AI use case、端口、任务、策略或 Provider 实现 | AI需求、技术边界 |
| `packages/contracts/src/index.ts` | 运行时/编译时共享 DTO、错误、包络、事件与幂等契约 | 任一跨层请求、响应、错误码、事件或序列化规则变化 | API 文档、前端 API 客户端、契约测试 |
| `packages/rules/src/` | 可结算纯规则、输入输出、版本与可重放行为 | 开包、报价、订单、卡组、赛事、成就等规则或参数变化 | 规则测试、后端用例、实施计划 |
| `apps/api/docs/adr/` | 已作出的重要且持久的技术/业务实现决策与取舍 | 选择或拒绝会影响多个模块、数据迁移、运维或安全边界的方案 | 技术边界、各 ARCHITECTURE |
| `apps/api/docs/api/` | 对外 HTTP 协议、错误语义、版本兼容说明 | 端点、认证、响应包络、错误码、幂等行为或 OpenAPI 变化 | contracts、后端需求 |
| `apps/api/docs/operations/` | 运行、同步、市场重定价、日切、比赛结算、订单、活动发布、用户补偿、备份、恢复、告警与人工处置步骤 | 新任务、数据源、管理命令、备份/恢复、部署、监控或故障处置变化 | 实施计划 I05/I14B/I17B/I22B/I23B/I25B/I30B/I31B/I32B |
| `progress/<迭代ID>.md` | 对应迭代实际新增、修改、删除的文件清单，各文件作用与迭代特殊点 | 对应迭代完成时，与迭代状态变更同步创建或更新 | 实施计划、验收证据、ADR、API/运维文档 |
| `README.md` | 项目概览、目录入口、最低启动路径和安全提醒 | workspace 入口、启动命令、开发前置条件或公开架构概览变化 | 技术边界、各应用 README |

## 代码与文档的同步矩阵

| 发生的变更 | 同步动作 |
| --- | --- |
| 新增/变更 API、DTO、错误码或幂等行为 | 更新 `packages/contracts`、`apps/api/docs/api/`、相关前端 API 调用和测试；若影响需求，更新前/后端需求。 |
| 新增/变更可结算规则 | 更新 `packages/rules` 与规则测试；记录规则版本；同步后端需求、验收定义和受影响迭代 checklist。 |
| 新增表、迁移、事务、不变量或异步任务 | 更新 API 架构、后端需求、运维文档、集成测试和实施计划；重大取舍新增 ADR。 |
| 新增或改变页面、状态归属、交互流程 | 更新 Web 架构、前端需求、主流程验收和端到端测试。 |
| 新增/变更 AI Provider、Schema、预算、降级或权限 | 更新 AI 架构、AI需求、运维文档、测试和 I28AI/I29AI/I32AI checklist。 |
| 变更部署、环境变量、数据卷、备份或监控 | 更新 `.env.example`、README、运维文档、技术边界和 I31B/I32B checklist。 |
| 完成迭代项或验收项 | 先保存测试/演示证据与必要的运维说明，再勾选实施计划中的对应项；同步主流程验收的执行状态；完成整个迭代时同步创建或更新 `progress/<迭代ID>.md`。 |

## 当前协作基线（2026-07-27）

- 当前实施计划状态：I01–I14F、I06F–I13F 已完成，下一可执行轨道为 I15B。I06B–I11F 的既有能力保持不变；I12B 已建立活动商店列表、购买预览、规则版本确认、幂等服务端开包、开包历史和 `pack_openings` 记录。迁移 `0013_base_bro_sos_packs.sql` 创建停用的 `BRO-BASE`、`SOS-BASE`，每次成功目录同步会在同一事务以当前系列非闪 Scryfall SKU 追加对应不可变规则快照并启用，缺少目录或必要稀有度时保持停用。I12F 以 TanStack Query 读取预览和历史，只回传服务端规则版本；同一网络重试复用幂等键，动画仅保存可丢弃的 UI 进度，结果只取 mutation 或历史 API 的服务端 DTO。一次成功购买会在单一 SQLite 短事务中完成 CSPRNG 重放审计、`pack_purchase` 账本、`pack_opened` 库存流水、`pack.opened` 事实事件/outbox 与审计日志；余额不足、下架/无效包、版本过期和库存写入故障不会留下半完成结果。I13B 已以 `prices.sync` 下载 MTGJSON `AllPricesToday`/`AllPrintings`，为 Scryfall SKU 追加 UUID/工艺映射及 Cardmarket EUR 快照，并在失败时保留 `price_sync_state` 的最近成功指针；无价、零价或映射失败 SKU 均标记为不可新增交易。迁移 `0015_price_sync_checksum_bypass.sql` 允许仅在稳定 `CHECKSUM_MISMATCH` 后由管理员确认并审计覆写，成功快照标为 `bypassed`。I13F 新增隔离的玩家公开状态和管理员同步状态页面：玩家只能读来源、最近成功更新时间、新鲜度及 SKU 可新增交易状态；版本、校验和、统计、任务和失败摘要仍只由 admin API 返回。I14B 以 `market/v1` 按整数规则从不可变 EUR 快照和已结算事实生成可重放的本服/NPC 报价；`market_quotes(sku_id, trigger_key)` 使同步和事实事件重放至多写入一次，过期/越界事件不会生效，失败保留最近报价。I14F 新增只读市场列表投影与 `/market`：页面从本地 API 读取外部锚点、游戏内/NPC 报价、指数、原因、来源和更新时间，筛选写入 URL；无价或缺少报价投影 SKU 明确禁用交易入口，过期和查询失败不会伪装为实时或可交易数据。库存查询读取最近成功报价投影作为旧持仓估值，不会改写库存事实或放开无价 SKU 的新交易。完成记录见 [progress/I06B.md](progress/I06B.md)、[progress/I06F.md](progress/I06F.md)、[progress/I07B.md](progress/I07B.md)、[progress/I07F.md](progress/I07F.md)、[progress/I08B.md](progress/I08B.md)、[progress/I08F.md](progress/I08F.md)、[progress/I09B.md](progress/I09B.md)、[progress/I09F.md](progress/I09F.md)、[progress/I10B.md](progress/I10B.md)、[progress/I10F.md](progress/I10F.md)、[progress/I11B.md](progress/I11B.md)、[progress/I11F.md](progress/I11F.md)、[progress/I12B.md](progress/I12B.md)、[progress/I12F.md](progress/I12F.md)、[progress/I13B.md](progress/I13B.md)、[progress/I13F.md](progress/I13F.md)、[progress/I14B.md](progress/I14B.md) 与 [progress/I14F.md](progress/I14F.md)。I06 阶段无 AI 实现任务。
- 2026-07-27 状态更新：I15B、I15F、I16B、I16F、I17B、I17F、I18B、I18F 已完成，下一可执行轨道为 I19B。I15B 建立 `GET /v1/npc-trades/buy/{skuId}/preview` 和幂等 `POST /v1/npc-trades/buy/{skuId}`；实际成交只引用不可变 `market_quotes` 快照和规则版本，服务端校验限价、余额、SKU 可交易状态、报价有效期及单笔/用户-SKU-UTC 日额度。迁移 `0017_npc_trades.sql` 追加 `npc_trades` 和受控额度；每次成功买入在同一事务写 `npc_buy` 账本、库存、成交、`npc.trade.settled`、outbox/重定价任务、审计和幂等响应。I15F 在市场表格仅为可交易且有报价的 SKU 开放买入弹窗，先按数量读取服务端预览，再回传不可变报价标识和单位限价确认；成功后只失效存档、库存、账本、报价和价格状态缓存，不在浏览器结算余额或库存。I16B 新增 `GET /v1/npc-trades/sell/{skuId}/preview?quantity=<n|all>` 和幂等 `POST /v1/npc-trades/sell/{skuId}`；`all` 仅解析当前可用库存，确认仍携带解析后的数量和最低可接受价。卖出在同一事务追加 `npc_sell` credit 账本与库存扣减、成交、`npc.trade.settled`、outbox/重定价任务、审计和幂等响应；锁定库存、报价下跌、同键异参、并发重放及写入故障均不能产生半完成成交。I16F 在库存页只展示 SQLite 服务端投影的单张现价、全部持仓市值与未实现盈亏；卖出弹窗支持指定数量与 `all`，确认只发送服务器预览解析出的报价标识、版本、数量与最低单价。同步确认锁阻止双击在 React 提交禁用生效前产生第二个 HTTP 请求，成功后仅失效服务器真相缓存；锁定量、数量不足及报价变化都不能被浏览器绕过。I17B 让价格历史可查询、每日同步可调度、失败可降级：`GET /v1/market/quotes/{skuId}/history?range=7d|30d|all` 与 `GET /v1/market/index/history` 按自然日采样只追加的 `price_snapshot_entries`/`market_quotes`；task runner 以 5 分钟节流日切轮询按 UTC 自然日唯一键调度 `prices.sync`（停机多日只补投一次）；新增 `prices.backfill` 任务下载独立 `AllPrices` 端点只补缺失历史日期（`run_kind='backfill'`），绝不覆盖每日同步、移动最近成功/调度指针或为历史日投递 `market.reprice`，checksum/解析/事务失败整笔回滚。迁移 `0018_price_history_and_schedule.sql` 增加 `price_sync_runs.run_kind` 列与 `price_sync_schedule_state` 单例；`PublicPriceStatusDto.disclaimer` 由服务端固定 MTGJSON/Cardmarket/虚拟货币/非实时/非真实资产说明。I17F 新增玩家路由 `/market/history`：`features/market/price-history-page.tsx` 只读上述两个历史端点并以 ECharts（`echarts@^5.6.0`）渲染双折线，金色为 Cardmarket EUR 参考价/指数、蓝色为游戏内报价/指数；缺失日 `connectNulls=false` 断线不插值，默认 30d，切换 7d/全部或选中 SKU 才把 `range`/`skuId` 写入 URL；每个图表区另渲染同名只读降级表格，`role="img"` 配合中文 `aria-label`。`freshness=stale` 仍展示旧快照指数与“沿用最近成功快照；这不是实时 Cardmarket 价格。”，空历史显示“该 SKU/范围暂无历史快照”，查询失败显示错误重试。I18B 建立 P2P 双边委托预览与创建：`GET /v1/orders/buy|sell/{skuId}/preview` 以 `market_quotes.market_price` 为锚点、`bilateral_order_limits.limit_price_band_bps` 计算限价带，`@mtg-market/rules` 的 `order/v1` 计算 order_fee 与 fulfillment_deposit 并派生 `previewVersion`；`POST /v1/orders/buy|sell/{skuId}` 只接受未过期 `previewVersion`/`quoteVersion`/`quoteId` 与玩家确认的限价，买单原子预占 数量*限价+order_fee（`fund_holds`），卖单锁定库存（`inventory_holds`）并只预占保证金（order_fee 留到 I19B/I20B 履约时扣除）。迁移 `0019_bilateral_orders.sql` 追加 `bilateral_order_limits` 与只追加语义的 `bilateral_orders`（状态机、乐观版本、引用不可变报价、预览版本与 `expires_at`）；同键并发重放、同键异参、余额/库存不足、额度、限价越界、预览/报价过期、撤单幂等释放、订单簿只读聚合与卖单库存写入异常回滚均有集成测试。`GET /v1/orders`、`GET /v1/orders/{orderId}`、`POST /v1/orders/{orderId}/cancel`、`GET /v1/orders/book/{skuId}` 完成我的委托、撤单与只读订单簿；撮合、模拟履约、`p2p.trade.settled` 与 `order.expire` 延后至 I19B/I20B/I22B。I18F 在市场页可交易 SKU 行接入「挂买单」、库存页可用持仓行接入「挂卖单」，统一弹窗 `features/orders/create-order-dialog.tsx` 复用同步双击锁与服务端预览，提交体严格为 `{quoteId,quoteVersion,previewVersion,quantity,limitPrice}`，`previewVersion` 为 64 位 hex 原样回传、`limitPrice` 为整数最小货币单位，费用/保证金/限价带/可用量只展示不回传；`/orders` 以服务端订单字段展示我的委托并按 status/side/cursor/limit 筛选，撤单经 `ConfirmDialog` 二次确认并以幂等键提交，`RESOURCE_CONFLICT` 提示刷新。同一网络重试复用幂等键、重新预览或任一输入变化才换键；`VERSION_STALE` 必须重新预览。`daily.rollover` 发钱/赛事刷新（AT-10B）延后至 I23B/I25B；AT-10A 的 checksum 覆写浏览器确认仍待 I13F。完成记录见 [progress/I15B.md](progress/I15B.md)、[progress/I15F.md](progress/I15F.md)、[progress/I16B.md](progress/I16B.md)、[progress/I16F.md](progress/I16F.md)、[progress/I17B.md](progress/I17B.md)、[progress/I17F.md](progress/I17F.md)、[progress/I18B.md](progress/I18B.md) 与 [progress/I18F.md](progress/I18F.md)。
- 2026-07-28 状态更新：I19B 已完成，下一可执行轨道为 I19F。`packages/rules` 新增 `order-matching/v1`：`matchOrders` 以纯函数按价格—时间优先（买单限价降序、卖单限价升序、同价按 sequence）、成交价取 maker（先入订单簿一方，createdAt 相同按 sequence）、部分成交与剩余数量推进，`checkSelfTrade` 跳过同用户买卖；相同输入恒产生相同 legs/remaining/skippedSelfTrade。`apps/api/src/modules/orders/application/order-service.ts` 的 `OrderService.match(skuId)` 以独立 `InventoryService.withLedgerTransaction` 短事务加载 skuId 下未过期 `open/partially_filled` 委托，调规则得 legs 后逐 leg 以条件 UPDATE（`WHERE version=? AND remaining_quantity>=?`）扣减双方剩余，把已成交部分的买方资金（release `order_buy` 全量 hold → reserve `order_fulfillment` 已成交 hold + 剩余 `order_buy` hold）、卖方库存（`SqliteInventoryRepository.capturePartial` 让已成交数量离开持有、原 hold 收缩到剩余）与卖方保证金（release 全量 → reserve 已成交 + 剩余）转为待履约持有，落 `bilateral_trades`（status=`matched_pending_fulfillment`、`UNIQUE(buy_order_id,sell_order_id,execution_price_amount)`）并写买卖各一条审计。`POST /v1/orders/{skuId}/match`（admin 角色保护）与创建成功后自动撮合是触发入口；撮合失败只记日志、不影响委托创建结果，运维可经 admin 端点重跑。并发撮合由 SQLite 短事务串行 + 条件 UPDATE + `bilateral_trades` 唯一约束保证至多执行一次、业务结果至多一次，不转移最终所有权、不写 `p2p.trade.settled`、不结算卖方收入/保证金（留 I20B）。迁移 `0020_p2p_matching.sql` 追加只追加语义的 `bilateral_trades`。契约新增 `BilateralTradeDto`/`MatchResultDto`/`BilateralTradeStatus`。`apps/api/src/modules/inventory/application/inventory-service.ts` 与 `infrastructure/sqlite-inventory-repository.ts` 新增 `capturePartialInLedgerTransaction`/`capturePartial`，让部分成交的库存捕获保留原 hold 供撤单释放剩余。AT-05B 服务端部分通过：全量/部分撮合、价格—时间优先、自成交拒绝、不写 `p2p.trade.settled`、admin 显式触发与普通玩家 403、并发不重复成交、撮合写入异常整笔回滚均有 `apps/api/src/modules/orders/api/order-routes.test.ts` 集成测试；撮合规则正常/边界/非法/部分成交/价格—时间优先/确定性有 `packages/rules/src/index.test.ts` 单测。AT-05B 浏览器部分待 I19F；模拟履约、`p2p.trade.settled`、`order.expire` 定时回收延后至 I20B/I22B。完成记录见 [progress/I19B.md](progress/I19B.md)。
- 2026-07-28 状态更新：I19F 已完成，下一可执行轨道为 I20B。为满足权威边界（浏览器不得推导成交/资产状态），新增只读服务端接口 `GET /v1/orders/trades`（player 角色）：`OrderService.listPlayerTrades` 从 `bilateral_trades` JOIN 卖单委托读取 `unit_fulfillment_deposit_amount`，投影为脱敏对手的 `PlayerBilateralTradeDto`（只返回当前玩家自己的 `myOrderId`、`role`（buyer/seller）、成交价取 maker、本方已成交 order_fee 与已转入待履约的资产：买方 `pendingFunds=数量×成交价+order_fee`、`pendingInventoryQuantity=null`；卖方 `pendingFunds=单位保证金×数量`、`pendingInventoryQuantity=已离开持有数量`），对手 userId、对手 orderId 与所有 holdId 一律不返回；纯读、不写事务、不引幂等键、不写审计。契约新增 `PlayerBilateralTradeDto`。`apps/web/api/orders-api.ts` 新增 `usePlayerTradesQuery` 与 `useOrderBookQuery` 的 10s 轮询（`refetchIntervalInBackground:false`、失败时不轮询），`useCreateOrderMutation`/`useCancelOrderMutation` 成功后失效 `["orders","trades",user.id]`。`features/orders/orders-page.tsx` 增加余额状态卡（`useArchiveQuery`）、双边订单簿面板（SKU 选择器默认最近未完成委托 SKU、可手动切换写入 URL `?skuId=`，买单降序/卖单升序、剩余数量与委托数、数据截至时间）、成交与待履约资产表格、待履约资产摘要；连接失败显示「数据可能过期，连接失败，正在重试」与「立即刷新」，恢复后只接受服务端最新状态。AT-05B 浏览器部分通过：双玩家部分/全部撮合订单簿一致、各自角色与待履约资产切分、脱敏对手、成交查询失败→恢复、空成交空态、窄屏自适应均有 `apps/web/tests/e2e/orders.spec.ts` 覆盖；全量/部分撮合、脱敏、分页与 skuId 过滤、自成交空列表有 `apps/api/src/modules/orders/api/order-routes.test.ts` 集成测试。履约确认/取消（I20B/I20F）、`order.expire` 定时回收（I22B）未上线前，待履约资产只可展示、不可操作。完成记录见 [progress/I19F.md](progress/I19F.md)。
- 2026-07-28 状态更新：I20B 已完成。`OrderService` 新增 `fulfill`/`cancelTrade`/`expireOrder`/`expireTrade`/`expireByPayload`，全部在 `InventoryService.withLedgerTransaction` 单短事务内经 users/inventory application 接口协作，不跨界写 accounts/fund_holds/inventory_holdings。确认履约（`POST /v1/orders/trades/{tradeId}/fulfill`，player，请求体为空、需 `Idempotency-Key`、买卖任一方均可发起）按成交价结算买方扣款：先 `releaseOrderFunds` 释放撮合时按买单限价预占的全量 `order_fulfillment` hold（限价>=成交价时差额退回买方 available），再 `spendAvailableFunds` 按 数量×成交价+order_fee 扣款；库存以成交价为成本 `acquireInLedgerTransaction` 转入买方；`releaseOrderFunds` 返还卖方保证金、`creditAvailableFunds` 卖方收入（数量×成交价-order_fee）；成交推进为 `fulfilled`；追加 `p2p.trade.settled` 事实事件 + outbox('market.fact-event') + `market.reprice` 任务（market-service 按 `liquidity` 消费一次 quantity）。取消履约（`/cancel`）`applyTradeCancellation`：`releaseOrderFunds` 退回买方全量待履约资金、`captureFunds` 扣除卖方已冻结保证金（账本 reason 取 hold.reason=`order_fulfillment_deposit`、correlation=`p2p-deposit-forfeited:{tradeId}`）、`restorePartialInLedgerTransaction` 把撮合时已 capture 离开持有的数量加回卖方 quantity/available；成交推进为 `cancelled`，不写 `p2p.trade.settled`。`order.expire` 任务到期回收：建单与撮合成交时分别投递 `runAfter=expires_at`/`fulfillment_deadline`、`uniqueKey=order-expire:{id}`/`trade-expire:{id}` 的任务（`(type, unique_key)` 唯一索引去重），到期把 `open/partially_filled` 委托转 `expired`（释放剩余预占）或 `matched_pending_fulfillment` 成交转取消履约；`POST /v1/orders/trades/{tradeId}/expire`（admin）显式触发，普通玩家 `403`。状态机条件 UPDATE（`WHERE status='matched_pending_fulfillment'`）保证已 fulfilled/cancelled 不重复迁移；并发与重放至多一次业务结果，同键同参重放返回首次响应、异参 `IDEMPOTENCY_CONFLICT`。履约期限沿用 `bilateral_order_limits.ttl_seconds`，由 `packages/rules` 的 `order-fulfillment/v1`（`ORDER_FULFILLMENT_RULE_VERSION`、`resolveFulfillmentDeadline`/`validateTradeFulfillment`/`validateTradeCancellation`/`isFulfillmentOverdue`）派生写入 `bilateral_trades.fulfillment_deadline`。迁移 `0021_p2p_fulfillment.sql` 追加该列与索引；契约 `BilateralTradeDto`/`PlayerBilateralTradeDto` 新增 `fulfillmentDeadline`、新增 `BilateralFulfillmentType`（本期单一 `simulated`，不引入实体物流）。`SqliteInventoryRepository.restorePartial` 与 `InventoryService.restorePartialInLedgerTransaction` 用于取消履约恢复已 capture 库存；`enqueueOrderExpireJob`（jobs application）封装投递；`task-runner.ts` 注册 `order.expire` handler。AT-06 服务端部分通过：正常履约、取消履约、幂等重放与同键异参 conflict、已终态状态机防护、无关玩家 404、撮合投递到期任务、成交到期回收与重复迁移防护、委托到期转 expired 释放预占、履约事务回滚资金/库存/保证金守恒均有集成测试；履约规则正常/边界/非法/确定性有规则单测。完成记录见 [progress/I20B.md](progress/I20B.md)。
- 2026-07-29 状态更新：I20F 已完成，下一可执行轨道为 I21B。`apps/web/api/orders-api.ts` 新增 `bilateralOrderApi.fulfillTrade`/`cancelTrade` 与 `useFulfillTradeMutation`/`useCancelTradeMutation`：以幂等键 + 成交 ID 提交空请求体（同键同成交重放返回首次结果，换成交才生成新键），成功只失效服务器真相缓存（orders/book/trades/archive/ledger/inventory/market/price 状态），不在浏览器结算余额/库存/保证金。`features/orders/orders-page.tsx` 把「我的成交与待履约资产」从只读升级为可操作：成交表格增加「状态」（按 `trade.status` 展示待履约/已完成/已取消）、「履约期限」列与「操作」列（仅 `matched_pending_fulfillment` 显示「确认履约」「取消履约」按钮，提交期间禁用）；新增 `TradeConfirmDialog` 二次确认弹窗，展示成交 ID、角色、SKU、数量、成交价、order_fee、履约期限、规则版本、服务端投影的待履约资金（买方=数量×成交价+order_fee、卖方=已成交保证金）与待履约库存、确认/取消后果说明，并在弹窗与摘要两处明确「不涉及实体卡牌物流、发货或退货」。`tradeActionLock` ref 阻止双击在提交禁用生效前发出第二个 HTTP 请求；成功横幅附带关联成交 ID（玩家可据此在玩家首页账本流水核对），`RESOURCE_CONFLICT` 失败显示服务端原因并提示刷新成交列表、不伪造成功。到期已由服务端（order.expire）推进为 `cancelled` 的成交显示「已取消」「不可操作」且无操作按钮。`packages/contracts/src/index.test.ts` 新增 `BilateralFulfillmentType` 固定为 `simulated` 断言。AT-06 浏览器部分通过：确认履约（双击只发一个请求、状态推进为已完成、关联成交 ID）、取消履约（买方视角退回资金、状态推进为已取消）、到期已不可操作、`RESOURCE_CONFLICT` 失败不伪造成功、窄屏二次确认与无物流声明均有 `apps/web/tests/e2e/orders.spec.ts` 覆盖；人工证据见 `apps/web/tests/manual/I20F.md`。端到端一致性恢复回归属 I22B；撮合风控属 I21B。完成记录见 [progress/I20F.md](progress/I20F.md)。
- 2026-07-29 修复更新：MTGJSON 报价刷新与流式下载修复（非迭代项，数据新鲜度/健壮性修复）。①`price-sync-service.ts` 投递 reprice 的 triggerKey 从 `price-sync:{runId}` 改为 `price-sync:{completedAt.slice(0,10)}`（下载完成的 UTC 自然日）；`market-service.ts` 默认 fallback 与之对齐，并把 `market_quotes` 的 `ON CONFLICT(sku_id, trigger_key) DO NOTHING` 改为 `DO UPDATE SET`（覆盖全部业务字段）——报价新鲜度取决于「本日是否成功 reprice 过」，不再耦合 MTGJSON `meta.date`；同日二次同步能纠错续期 `valid_until`，业务结果按 SKU 维度至多一次。②`mtgjson-client.ts` 全部三个载荷（AllPricesToday/AllPrintings/AllPrices）改流式：`response.body → 写 TMPDIR 临时文件 + 边下载边算 SHA-256`（下载失败/截断重试 3 次），`createReadStream → 按 gzip 魔数决定 gunzip → stream-json（Pick+StreamObject/StreamValues）` 逐对象解析，杜绝 `gunzipSync`+`toString` 触发的 V8 字符串上限 `0x1fffffe8`（AllPrices 回填命中根因）；新增 `stream-json`+`stream-chain` 依赖。③卡住的 `prices.sync` job 由 task-runner `recoverExpired` 在每次 claim 时自愈（lease 10 分钟过期后重置 pending 重跑），无需手动改库。新增 `mtgjson-client.test.ts` 覆盖 gzip/未压缩两路流式解析等价性、checksum 不匹配/bypassed、缺失 cards；`market-service.test.ts` 幂等测试改为覆盖语义（同日重放 `[1,1]` + 覆盖 valid_until、跨日保留）；`price-sync-service.test.ts` 新增 reprice triggerKey 形如 `price-sync:YYYY-MM-DD` 断言。`pnpm check` + 110 个 api 测试全过。
- 现有工程基线：pnpm workspace、Next.js 页面骨架、共享 contracts/rules、持久化 SQLite 串行任务 worker、AI Schema/模板骨架，以及数据库基础表、迁移与事务原语；API 已具备统一响应包络、请求 ID、健康检查和最小 OpenAPI 文档端点。
- 尚未建立的正式记录：ADR。版本化 API 文档和运维手册已从 I04 开始建立，后续端点和任务应按索引同步扩展。
- 2026-07-23 文档审查修订：D9 双边委托、E7–E11 市场动态、资金冻结、缺失任务类型、分阶段验收与自动化测试门禁已纳入实施计划；保底、AI 赛前文本、模拟履约和移动端范围冲突已统一。
- 2026-07-24 前端与管理端审查修订：新增逐期页面/Playwright/人工验收门禁；H5 提升为 MVP 必须，补齐管理员登录后查看脱敏日志、版本化发布活动、检索/冻结玩家、撤销会话及补偿修正流程，并新增 AT-13。
- 2026-07-24 责任轨道修订：I01–I05 保留原编号；I06 起按实际交付拆为 `IxxB`、`IxxF`、`IxxAI`，不适用的 AI 轨道不创建空迭代。实施计划只以技术模块职责、主流程和产品计划书为拆分依据，各需求文档单向引用轨道 ID。
